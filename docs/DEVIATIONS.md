# SENTINEL — Deviations from Frozen Architecture

This file records every intentional deviation from `docs/ARCHITECTURE.md`.
Numbers are stable identifiers; a resolved deviation keeps its number and is marked as removed.

| # | Section | Deviation | Reason |
|---|---------|-----------|--------|
| 1 | — | *Removed.* The backend now runs on Python 3.12.2 as specified (`requires-python = "==3.12.*"`). | Phase 1 review B1: the lockfile installed unchanged on 3.12 and all tests passed. |
| 2 | Non-negotiable 16 | Dependency pins are the latest versions available when the project was set up (for example pandas 3.0.5, scikit-learn 1.9.1), not versions named in the contract. | The contract asks for exact pins but names none. `tests/unit/test_environment.py` asserts `sklearn.__version__` equals the pin so artifacts cannot be loaded under a different version silently. |
| 3 | §1 (`uv.lock`) | Lockfile is `backend/requirements.lock` from `pip freeze --exclude-editable`, not `uv.lock`. | `uv` is not installed on the dev machine. Install with `pip install -r requirements.lock` then `pip install --no-deps -e .`. |
| 4 | §1 | Money helpers live in `backend/sentinel/money.py` (`format_inr`, `make_money`). | Not in the §1 tree. Implements §4 "formatted once, server-side" and the §14.1 single `format_inr()` rounding rule in one place. |
| 5 | §3 | `decisions_core_immutable` protects every column except `current_action`, `status`, `latest_audit_event_id` (the contract lists 10 columns). Added CHECK constraints: `decisions.cost_optimal_action / recommended_action / current_action` and `audit_events.new_action` in the action set, `previous_action` NULL or in it, `selected_rule` in the three rules. | Strengthens the stated immutability and action vocabulary; no column, table or name changed. |
| 6 | §4 | `PolicyDecision` has a model validator: outside `DEGRADED_MODE_FALLBACK`, all four actions exactly once, PREPAID_ONLY and MANUAL_REVIEW feasible, selected feasible and within ₹1 of the feasible minimum, `cost_optimal_action` within ₹1 of the overall minimum, and `selected == cost_optimal` iff `selected_rule == "MIN_EXPECTED_COST"` (degraded rules: see #14). `CheckoutRequest(ScoreOrderRequest)` added for the public route. `QueueItem.status/source` and `OrderSummary.delivery_speed` narrowed from `str` to the Literals used elsewhere in §4. | Enforces §9.1 invariants at the contract boundary. |
| 7 | §2 (`/health`) | `/health` (not in §4) returns `policy_version`, the first 8 chars of `policy_config_sha256`, and `demo_clock`; the sidebar renders the policy version from it. | Avoids a hard-coded "v1.0" in the UI. No money, scores or thresholds exposed. |
| 8 | §8.4 | Validator also rejects `floor > cap`, negative `*_inr`, `tie_tolerance_inr < 0`, `block_min_corroborating_signals < 2`, `high_exposure_min_p_abuse` outside `(0, block_min_p_abuse)`, and missing or unknown keys. The slope-order check returns the PREPAID/REVIEW swap region as **info**; any other adjacent slope-order violation is an error. | Review items B13 and the Phase 1 brief. |
| 9 | §1 (frontend) | `vite.config.ts` declares `process` locally instead of adding `@types/node`. | Keeps the dependency list unchanged while injecting `X-Internal-Key` via the Vite proxy from `SENTINEL_INTERNAL_API_KEY`. |
| 10 | §9.1 | `argmin_with_tiebreak` takes the cent-rounded totals (`make_money(total).inr`) rather than raw `CostBreakdown.total`. | Rounding happens before the ₹1 tie comparison so the choice always agrees with the `Money` values stored in the decision and checked by the `PolicyDecision` validator. |
| 11 | §9.1, §9.3 | Policy inputs are a `DecisionContext`: `p_abuse`, `p_return` (record only), V, CLV, the five `EvidenceSignal`s, `decided_at`, `graph_state_as_of`, degraded flag. §9.2 signals are derived by `corroborating_signals(SignalInputs)`, where `SignalInputs` holds the §6.3 feature values plus a link weight per signal and the link kinds carrying any burst. The policy package never reads `sentinel.settings` or the wall clock (AST test). | The contract names the inputs but not their container. The caller supplies `decided_at`: `DEMO_CLOCK` when serving, `placed_at` in the backtest. |
| 12 | §9.2 | `present` = the evidence was observed; `counts_for_corroboration` = it also meets the §9.2 qualifiers. ADDRESS on a multi-tenant address and TEMPORAL_BURST reached only through address links are present but not counted, keep their weight, and say why in `detail`. ACCOUNT_CLAIMS weight is 1.0 (account evidence, not a decayed link; recency is the 180-day window). | Phase 1 review B2/B3: discounted evidence must stay visible to reviewers. |
| 13 | §9.3 G4 | G4 compares `graph_state_as_of` with **`decided_at − 24 h`** (the contract says `DEMO_CLOCK − 24 h`), and its weakness check runs over counted signals only; with no counted signal G2 already removes BLOCK. | Phase 1 review A1: with the fixed demo clock, every backtest decision (D306–D365) lost BLOCK. §11 Demo 3 lists BLOCK removed by G2 and G3 only. |
| 14 | §3, §4, §9.1 G6, §10 | Degraded mode means no model score exists, and none is invented. `DecisionContext.p_abuse/p_return` are None when degraded (and required otherwise); `graph_state_as_of` may be None when degraded. `Scores.p_return/p_abuse`, `QueueItem.p_return/p_abuse` and `PolicyDecision.cost_optimal_action` are nullable; a degraded `PolicyDecision` must have `costs == []` and `cost_optimal_action is None`. `decisions.p_return`, `p_abuse`, `cost_optimal_action` are nullable with `CHECK ((degraded_mode = 1) OR (all three NOT NULL))`. For Phase 6: `AuditEventPayload.p_return`, `p_abuse`, `cost_optimal_action` must be optional. UI (Phase 8): show "No model score, degraded mode" and hide the cost chart. | Phase 1 review B4: costs "calculated" from a placeholder probability would be a deceptive record. `QueueItem` is included so degraded decisions can be listed. |
| 15 | §9.5 | `allow_all()` returns an `Action`, not a `BaselineOutcome` (§4 `BaselineOutcome.strategy` allows only FIXED_THRESHOLD and RULE_BASED). RULE_BASED thresholds are module constants, not config keys. FIXED_THRESHOLD takes τ as arguments; tuning on CALIBRATION is Phase 4. | Keeps §4 and the config key set unchanged. |
| 16 | §1 (frontend) | `react-router-dom` (7.18.4) is a dependency. All frontend dependencies are pinned to exact versions, `frontend/.npmrc` sets `save-exact=true`, and installs use `npm ci`. | Three routes need a router; §1 omitted it. Exact pins stop `npm install` pulling newer React Flow, Recharts or Tailwind before the demo. |
| 17 | §5 (Generator outputs) | The generator writes one Parquet file per table in `backend/data/`: `events.parquet` holds `order_events`; `accounts.parquet`, `identifiers.parquet`, `orders.parquet` and `order_lines.parquet` sit beside it, with `sim_ground_truth.parquet` and `labels.parquet` as specified. Timestamps are stored as UTC `datetime64`. | A Parquet file holds one table, so the five tables §5 lists under `events.parquet` cannot share one file. Approved by the project owner in Phase 2. |
| 18 | §7.1, §3 `order_labels` | An order that was never delivered (RTO, CANCELLED, or not yet delivered at `as_of`) gets `abuse_status = NOT_MATURED` with `abuse_label` NULL, alongside the NULL return label. | `abuse_status` is NOT NULL and no other value fits: the abuse label matures at delivered + 60 d, which never happens. Keeps `NO_CLAIM` meaning `abuse_label = 0`. Approved by the project owner in Phase 2. |
| 19 | §7.1 (Unresolved) | `UNRESOLVED` (label NULL, excluded) also covers a `QC_FLAGGED` return with no resolution, and any dispute whose `ABUSE_CONFIRMED`/`ABUSE_CLEARED` arrives after delivered + 60 d. Resolutions are only counted inside the adjudication window. | §7.1 defines unresolved only for claims. A QC flag is an open dispute, and without this rule uninvestigated flagged abuse would silently become label 0. Approved by the project owner in Phase 2. |
| 20 | §5 (Archetypes, Outcome generation) | Generator parameters the contract does not set, all named constants in `data/archetypes.py` and `data/generator.py`: **investigation coverage** (Phase 2 review, Fix 2, replacing §5's ring 0.70 / opportunistic 0.50) is 0.95 for a dispute backed by QC_FLAGGED or by carrier evidence that contradicts the claim and 0.60 for any other dispute, for every archetype; 5 % of investigated disputes never resolve; carrier evidence contradicts an abusive claim with p 0.95 (raised from 0.75 in 0.05 steps; 0.95 is the lowest rate giving ≥ 40 CALIBRATION positives with C1–C4 passing) and a genuine one with p 0.02; claims are filed delivered + U(0.1, 5) d; 1 % of orders are CANCELLED; uninvestigated claims are REFUNDED; **rings R1–R3** (Fix 1): 24 / 36 / 30 members, each placing 3–6 orders within 1–6 days of its first, all inside the ring window; no orders outside the window. **R1 and R2 recruit in waves** (Phase 4 decision B1): R1 waves start D40, D60, D80 (8 / 8 / 8 members), R2 waves D170, D195, D220, D245 (9 / 9 / 9 / 9); a member's first order falls in [wave start, wave start + 3] and its orders span ≤ 10 days; each wave has its own devices and tokens (the ring's 6/4 and 9/6 spread evenly over its waves) and every later wave reuses the first wave's first device and first token through 3 members who start on the same day, redrawn until ≥ 2 of them use each (at most 200 draws); R3 has no waves; 6/4, 9/6, 8/5 shared devices/tokens (member m uses device m mod n); shared device on 85 % of orders, shared card on 75 % of abusive card orders; payment mix card 0.40 / UPI 0.40 / COD 0.20; 70 % of abusive orders on one of two ring target products drawn from the ₹8,000–₹40,000 pool; half the members are dormant recruits (account created 90–1,500 d before its first order, no history); R3 opens with a launch burst (half its members start in D315–D318); members rotate through 4 drop addresses of their own; **high-value genuine carts** (Fix 3): 12 % of NORMAL and 15 % of FREQUENT_RETURNER orders are one ELECTRONICS/FOOTWEAR/ACCESSORIES item priced ₹8,000–₹40,000 (discount dropped if it would take the value below ₹8,000), with the archetype's usual outcome process; 25 % of households also share a family device; hostel juniors inherit a senior's phone after ≥ 70 idle days; refurbished-device prior owners are generated as extra NORMAL accounts (2,090 + 60 ≈ 2,150); UNCONFIRMED_ABUSER abuse is undetected wardrobing (QC flag at the genuine 0.02 rate, never investigated), so it is labelled 0 as §5 requires. `sim_ground_truth.archetype` values: NORMAL, FREQUENT_RETURNER, HOUSEHOLD, OFFICE_HOSTEL_PG, REFURB_DEVICE, OPPORTUNISTIC, UNCONFIRMED_ABUSER, RING (with `ring_id`), DEMO (the three demo accounts). Synthetic account and order ids are `ACC-`/`ORD-` + an HMAC prefix of the generator key, so ids carry no archetype order. | The §5 table fixes counts and behaviours but not these rates; the Phase 2 review set coverage, ring scale and high-value carts. Result with seed 20260901 (after the Phase 4 waves and demo changes): 2,940 accounts, 10,150 orders, return rate 23.0 %, confirmed abuse 3.5 % of orders, abuse positives TRAIN 181 / CALIBRATION 37 / TEST 97, UNRESOLVED 79 of 495 disputed orders (16.0 %), 1.7 % of ring orders with an own flagged claim in the prior 180 d (before the waves: 10,164 orders, 22.9 %, 3.7 %, 174 / 45 / 97, 75 of 504, 2.3 %); non-graph TEST abuse PR-AUC 0.555 (see #23). |
| 21 | §5 (R4), §11 | Ring R4's timing is hand-scheduled (`R4_CONFIRMED`, `R4_SCHEDULE` in `archetypes.py`): members 1–3 have QC-flagged returns confirmed on D356, D359 and D362, and every other R4 order is placed on or after D359, so no other R4 confirmation can land before `DEMO_CLOCK`. Carts, prior orders and post-clock outcomes are still sampled. Demo 3's address peer last shipped to the address 150 days before the demo order (link weight 0.04, as §11 states) and was confirmed 125 days before it. The three demo orders are built by `demo_requests()` and are not part of the generated history. | §11 needs exact Demo 2 context (5 device peers, 3 confirmed D356–D362, 3 token peers in 14 d, 4 linked orders in 24 h) that sampling cannot guarantee. For Demo 3, §11's weight 0.04 needs `last_seen` 150 days back, and a confirmation must follow its order. Phase 3 demo fixes (hand-written histories only; no feature definition changed): sampled R4 carts draw from the earbuds and the ₹8,000–₹40,000 pool and never the Demo 2 phone, which appears only on scheduled rows, so Demo 2 sees exactly 3 same-SKU linked orders in 7 d (it saw 8); Demo 1's weekly shoe line is a ₹1,499 pair (`PRD-FOOTWEAR-D03`), putting its CLV at ₹30,616 (was ₹37,276); Demo 3's D305 order is the ₹5,999 earphones instead of a kettle, putting its CLV at ₹15,710 (was ₹11,930). Phase 4 band fix (decision C): Demo 3's device peer is a recent account (created D356) whose first use of the device is D361, within 7 days of the demo order, with orders on D361 and D364 (was created D200 with orders D210–D354); still unconfirmed and concurrent. Demo 3 p_abuse moved 0.046 → 0.114 (band [0.20, 0.70] still missed). Phase 2 open questions, decided (Phase 2 review, Fix 7): Demo 1's order value is ₹4,500.01 (3 × ₹1,666.67 less 10 %), accepted as §11's ₹4,500; Demo 1's token is used only by this account since D1 of the simulation, not for 3 years, accepted. |
| 22 | §5 (Timeline, Rings), §3 `orders.split` | Orders of every hand-authored demo account (the three DEMO-archetype accounts, 59 orders, and since Phase 4 decision F1 their supporting peers `-HH`, `-DEV`, `-ADR`: 13 orders) stay in the world, so the graph and account history see them, but carry `split = "RECENT"` and are never in TRAIN, CALIBRATION or TEST. R3 "exists only in TEST" is enforced on activity: every R3 order is in TEST, no R3 identifier appears on a TRAIN or CALIBRATION order, and no R3 member is confirmed before the first 30 % of R3 orders are placed; a dormant R3 recruit's account may be created before D306 but has no earlier order. Identifier hashing lives in `features/identifiers.py` (normalisation: NFKC, trimmed, whitespace collapsed, case kept; placeholder denylist: empty, unknown/null/none/n/a and similar, all-zero or all-`f` values, and their hashed forms) and the generator imports it. | Phase 2 review Fixes 1, 5 and 6: demo histories must not leak into model splits, and serving code must hash identifiers without importing the generator. |
| 23 | §5 (Outcome generation), §7.1 (Unresolved), §13.3 | Phase 2 review-bound corrections. **C4 bounds set from Fix 2's expected unresolved rate (~19 %)**: UNRESOLVED / disputed orders ≤ 0.20 overall and ≤ 0.25 in TRAIN, CALIBRATION and TEST; RING + OPPORTUNISTIC share of UNRESOLVED ≤ 0.70 (the brief's ≤ 0.10 / ≤ 0.15 could not hold: 0.95 coverage × 0.95 resolution leaves ≥ 9.75 % of evidence-backed disputes unresolved, and 0.60-coverage disputes ~43 %). The §5 rule that 5 % of investigated disputes never resolve is unchanged. **C5 bound relaxed to non-graph TEST abuse PR-AUC ≤ 0.60; measured 0.578** at carrier rate 0.95 when Phase 2 was committed, and **0.555** after the Phase 3 demo-history fixes (#21). No carrier rate in 0.75–0.95 gave both ≥ 40 CALIBRATION positives and PR-AUC ≤ 0.50 (0.95 is the only rate with ≥ 40 positives), and the owner's 0.55 fallback is also exceeded; no further generator change was made. C1 (ring orders with ≤ 1 own flagged claim; 2.3 % have any) remains the primary guard that rings are hidden at account level. **Carry forward to Phase 4**: the evaluation must report the unresolved sensitivity check, with all metrics recomputed treating UNRESOLVED as 0 and again as 1. | Owner decision at the Phase 2 review gate: the original C4 bound was a brief error, not an implementation error. |
| 24 | §6.1–§6.3, §7.2 | Feature-builder choices the contract leaves open (all in `features/`): `feature_set_version` is `fs-1.0-` + the first 8 hex chars of SHA-256 over the sorted RETURN and ABUSE lists (currently `fs-1.0-0ad43d7f`). `account_age_days` is fractional days. The query order's own identifiers count as used by its account at t0 (age 0) for components and sequential-use checks, but are added to the graph only after features are computed. A link's weight is the other account's edge weight on the shared identifier; sequential device use is decided per account pair (the earlier user's last use ≥ 60 d before the later user's first use). Component size, the 200 BFS cap and `min(size, 200)` count accounts (query account included); hops are account hops. **Self-confirmation is not graph evidence** (Phase 3 review): `confirmed_abuse_proximity` measures hops to *other* confirmed accounts only, and `component_abuse_ratio_smoothed`, `device_confirmed_abuse_weight` and the address confirmed-abuse weight leave the query account out of the confirmed count (the ratio's denominator, the component size, is unchanged); an account confirmed before t0 with no confirmed neighbour has proximity 0 and ratio (0 + 1) / (n + 10). The §11 demo feature values are unchanged by this rule. `linked_orders_24h` and `linked_same_sku_7d` count orders by other accounts in the reliable component (Demo 2's 4 linked orders include a member reached in two hops). `device_confirmed_abuse_weight` and the address confirmed-abuse weight have no age window beyond decay; `token_other_accounts_30d` and `address_other_accounts_weighted_30d` use the other account's `last_seen`; `identifier_reuse_velocity_7d` counts every account's first use of the order's identifiers in 7 d. §9.2 link weights are the strongest link per signal; `burst_link_kinds` are the identifier kinds on the BFS paths to the accounts behind the burst, and `burst_weight` the strongest of those paths' weakest edges. Discounted links: at most one per identifier, by precedence HIGH_FANOUT_IDENTIFIER (weight 0), MULTI_TENANT_ADDRESS, SEQUENTIAL_DEVICE_USE, HOUSEHOLD_PATTERN, STALE_RELATIONSHIP. CLV "not-returned" excludes any order with a return or exchange visible at t0; tenure is account age. `features_for_request` refuses a builder whose state already holds anything at or after t0. `definitions.py` also names the graph feature group (`ABUSE_GRAPH_FEATURES`) for the §6.5 counterfactual. `build-features` reads the generator's Parquet files from `backend/data/`. | The contract names the features and rules but not these details; each choice is the narrowest reading that meets the §11 demo values. |
| 25 | §1, §4, §6.4, §9.5, §13.3 | Phase 4 choices the contract leaves open. **Files**: `build-features` also writes `backend/data/policy_inputs.parquet` (SignalInputs fields, `clv_inr`, `graph_state_as_of` = t0, raw `matured_return_rate`, `matured_returns`, `payment_method`, `order_value_inr`; policy and baseline inputs, never model features); `evaluation/report.py` assembles `evaluation.json` and `evaluation/demos.py` scores the demo requests offline until the Phase 6 `ScoringService` exists (`cli score-demos`). **Bundles** also hold `name`, `label` and `hgb_params`; `trained_at` is `DEMO_CLOCK`. **Metrics**: PR-AUC is average precision (step-wise, one point per distinct score; a numpy reimplementation tested equal to `sklearn.metrics.average_precision_score`); the CI is the 2.5/97.5 percentile of 1,000 row resamples (seed 20260901), skipping resamples without a positive; ECE bins split the sorted predictions into 10 equal-count bins; cohorts without a positive omit their PR-AUC keys; ₹10,000 exactly falls in ₹2–10k. **Backtest**: the abusive loss is scaled by `returned_value_fraction` only when `return_label = 1` (labels store 0.0 for item-not-received claims) and the review cost is not scaled; no abusive TEST order has a partial return, so this changes no reported number. `cost_per_detected_abuse` and `precision_block` are reported as 0 when undefined, with a note in `backtest_details`. FIXED_THRESHOLD ties go to the higher τ_block, then the higher τ_review; both τ come from the 0.05–0.95 grid, so the baseline always blocks at p ≥ 0.95. Cost sensitivity re-decides SENTINEL and re-tunes FIXED_THRESHOLD on CALIBRATION under each scaled config. Label sensitivity relabels UNRESOLVED rows at evaluation time only (models are not retrained; return metrics do not use the abuse label). Cold-start recall uses calibrated p_abuse ≥ 0.5; separation uses every FREQUENT_RETURNER TEST order. "No DEMO-account orders" covers every hand-authored demo account (#22). **Phase 4 decisions**: (A) review-bound correction: TEST return PR-AUC must exceed 1.5 × prevalence (was 2 ×); the return model does not drive actions (G1), a score using the true archetype reaches only 0.391 and the frozen return features reached 0.349 (0.386 after B). (B2) §6.4 parameters gain `monotonic_cst`, aligned with the bundle's feature order and stored in each bundle and in `model_registry.json`: abuse +1 on prior_suspicious_claims_180d, device_other_accounts_30d, device_confirmed_abuse_weight, token_other_accounts_30d, address_other_accounts_weighted_30d, component_abuse_ratio_smoothed, confirmed_abuse_proximity, linked_orders_24h, linked_same_sku_7d, identifier_reuse_velocity_7d, component_recent_claims_30d, every other abuse feature 0; return +1 on matured_return_rate_smoothed, prior_returns_90d, n_variants_same_product, every other return feature 0; no −1 (more evidence must never lower risk; removes the non-monotone identifier_reuse_velocity_7d artefact 0 → 0.69, 1 → 0.84, 2 → 0.66; keeps Phase 5 ablation reason codes coherent; applied regardless of demo outcomes). The evaluation-only no-graph model uses the same map restricted to its features. (F3) FIXED_THRESHOLD grid τ_review, τ_block ∈ {0.05, 0.10, …, 0.95, 1.0} with τ_review ≤ τ_block; 1.0 means never (`fixed_threshold` treats a threshold of 1.0 as never firing). (F2) the registry records `os.cpu_count()` at training; the committed-hash reproduction test runs only on the same count. (D) `guardrail_cost`: SENTINEL_UNCONSTRAINED is the expected-cost argmin over all four actions (checked equal to `decide()`'s cost_optimal_action); per guardrail, an order counts under every guardrail that removed its cost-optimal action, with the multi-guardrail count reported. (E) `calibration_bands`: Wilson 95 % intervals; the equal-width ECE uses 10 equal bins spanning [0.05, 1.0] (last bin inclusive) over rows with p ≥ 0.05. **Phase 4 blocker decisions (architect).** (Blocker 1, decision) `test_split_positives`' CALIBRATION bound is lowered from 40 to **≥ 35**; measured 37. The R2 wave 3 schedule is deliberately NOT moved: 37 vs 40 positives makes no material difference to a one-parameter sigmoid calibrator, and regenerating to gain three positives would put the passing Demo 2 at risk. TEST stays at ≥ 70 (measured 97). (Blocker 2, decisions 2.1-2.3) The count bound "TRAIN positives with confirmed-neighbour evidence ≥ 40" was a proxy for the property that the trained abuse model responds to that evidence. Measured directly: calibrated p_abuse is **exactly flat** (delta 0.0000) across the full range of both `device_confirmed_abuse_weight` and `confirmed_abuse_proximity`, on the Demo 2 feature row and on 200 seeded TEST rows, in both the wave-fixed world (TRAIN evidence 52) and the shipped world (21). **The behaviour test is therefore not added** (2.1), and the count test is kept only as a data-health check at **≥ 20** (2.2); measured **21** in the shipped world. **Phase 4's main finding** (2.3): the two features are individually predictive - fitted alone on TRAIN they drive p_abuse **0.026 → 0.800** with 393 and 272 splits - yet receive **zero splits in the full model**, and still zero with the correlated component features dropped or with **`min_samples_leaf` = 5**. `account_age_days` (843 splits), `component_size_reliable_90d` and `primary_category` identify the same rows first (correlation with `component_abuse_ratio_smoothed` **0.76** and **0.86**). The evidence is **redundant, not absent**; no generator change reaches the property while the cheaper proxies remain, and §6.3 is deliberately left unchanged. Phase 5 handles the consequence: see #27. (Decision 3, wave fix) `WAVE_REUSE_DEVICE_MEMBERS` was introduced to widen the reused device from the 3 token-reuse members to 6 per later wave, then **reverted to `WAVE_REUSE_MEMBERS` (3)**; the constant remains so the widening is one edit away. **The shipped world is the reverted one.** Side by side - reverted (shipped): R3 cold-start recall **0.727**, graph uplift **+0.287**, abuse TEST PR-AUC 0.814, TRAIN evidence count **21**; wave-fixed: R3 recall **0.494**, uplift **+0.269**, PR-AUC 0.796, TRAIN evidence count **52**. R3 cold-start recall is a headline demo number and the evidence count only ever proxied the test dropped in 2.1, so the recall wins. | The Phase 4 brief fixes the metrics, strategies and fields but not these details. |
| 26 | §11 (Demo 3) | **Demo 3's device fact amended by the architect.** "Device shared with 1 other account (not confirmed), concurrent" becomes **"Device shared concurrently with 3 other accounts in the last 30 days, none of them confirmed abusive."** Applied in `demo_orders.py` only: two more hand-authored peers, `ACC-DEMO-003-DEV2` (created D320, orders D340 and D352) and `ACC-DEMO-003-DEV3` (created D345, orders D355, D358 and D363), each created within 60 days of the demo order, each with 1-3 kept orders, first device use within 30 days of it, none confirmed, neither sharing Demo 3's address or token; all three peers stay split RECENT (#22). Three peers alone did not reach the band (p_abuse 0.0761): `device_other_accounts_30d` **saturates at 1** in the trained model, so the 2nd and 3rd shared account are worth nothing to the score. The lever that works (architect decision 1) is **one existing peer order moved into the 24 h window before the demo order** - the first peer's second order, D364 → D365 20:45, 13.7 h before the demo order at D366 10:25 - giving `linked_orders_24h` = 1. Exactly one order is moved, so TEMPORAL_BURST (which needs ≥ 3) stays absent. No new peers and no retrain: the peers are outside the model splits and TRAIN/CALIBRATION/TEST rows are unchanged (world 2,940 → 2,942 accounts, 10,147 → 10,152 orders, TEST abuse positives 97 throughout). **All four required outcomes hold.** (1) p_abuse **0.6913**, band [0.20, 0.70] - trail 0.046 (Phase 3) → 0.114 (Phase 4 decision C) → 0.0761 (3 peers) → **0.6913** (lever). (2) selected action **MANUAL_REVIEW** (cost-optimal, MIN_EXPECTED_COST). (3) counted §9.2 signals exactly **DEVICE and ACCOUNT_CLAIMS**; **BLOCK is removed by G3 only**, G2 now passing on two counted signals with an anchor. Full guardrail list: **G3, G5** - G5 removes ALLOW on high exposure, which the amendment did not anticipate but which does not affect condition 3 (it concerns BLOCK) and strengthens conditions 2 and 4. (4) margin (ALLOW ₹7,155.41 − MANUAL_REVIEW ₹1,857.52) / ₹1,857.52 = **+2.85**, required ≥ +0.15. Band headroom is thin at the top: 0.6913 against a 0.70 ceiling. | Architect's §11 amendment at the Phase 4 review: at one shared account no lever inside the old facts reaches the band, and the ALLOW-vs-REVIEW margin (₹994 vs ₹1,175) was too thin to be safe on stage. |
| 27 | §6.5, §11, UI | **Reason codes are evidence statements, not attribution statements** (architect, Phase 4 decision 2.4; **for Phase 5 to implement, recorded here only**). A code fires when its catalog predicate is true, regardless of its individual ablation delta. Every code displays its per-feature delta honestly; where that delta is ~0 the code carries the note *"redundant with other relationship evidence; the score is already explained by correlated features"*. Demo 2's confirmed-device and token-reuse codes therefore fire on evidence, and the **group** ablation (all graph features to reference) is what quantifies the relationship evidence as a whole. The order-detail UI separates two panels: **Evidence** (facts; drives guardrails G2/G3) and **Model attribution** (what moved the score). | Direct consequence of the #25 redundancy finding: the confirmed-device link is real and is exactly what G2 counts, so suppressing the code because a correlated feature absorbed the split would hide true evidence from the reviewer. |

## Withdrawn review requests

Requests from a review that were considered and then withdrawn. They are not deviations; they are recorded so the decision is not revisited.

| Review | Request | Decision |
|--------|---------|----------|
| Phase 1 review, C7 | Demo 1's policy explanation should contain no "not permitted" text. | **Withdrawn.** It conflicts with the §6.5 explanation style, whose Demo 3 example reports "BLOCK was also not permitted" although BLOCK was not a contender, and with C7's own Demo 2 expectation ("ALLOW was also not permitted"). Demo 1 has BLOCK removed by G2 and G3 in the same way, so the explanation keeps the §6.5 style and reports it. `test_c7_demo_1_explanation` asserts the full sentence. |
