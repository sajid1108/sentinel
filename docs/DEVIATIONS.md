# SENTINEL — Deviations from Frozen Architecture

This file records every intentional deviation from `docs/ARCHITECTURE.md`.
Numbers are stable identifiers; a resolved deviation keeps its number and is marked as removed.

| # | Section | Deviation | Reason |
|---|---------|-----------|--------|
| 1 | — | *Removed.* The backend now runs on Python 3.12.2 as specified (`requires-python = "==3.12.*"`). | Phase 1 review B1: the lockfile installed unchanged on 3.12 and all tests passed. |
| 2 | Non-negotiable 16 | Dependency pins are the latest versions available when the project was set up (for example pandas 3.0.5, scikit-learn 1.9.1), not versions named in the contract. | The contract asks for exact pins but names none. `tests/unit/test_environment.py` asserts `sklearn.__version__` equals the pin so artifacts cannot be loaded under a different version silently. |
| 3 | §1 (`uv.lock`) | Lockfile is `backend/requirements.lock` from `pip freeze --exclude-editable`, not `uv.lock`. | `uv` is not installed on the dev machine. Install with `pip install -r requirements.lock` then `pip install --no-deps -e .`. |
| 4 | §1 | Money helpers live in `backend/sentinel/money.py` (`format_inr`, `make_money`). | Not in the §1 tree. Implements §4 "formatted once, server-side" and the §14.1 single `format_inr()` rounding rule in one place. |
| 5 | §3 | `decisions_core_immutable` protects every column except `current_action`, `status`, `latest_audit_event_id` (the contract lists 10 columns). Added CHECK constraints: `decisions.cost_optimal_action / recommended_action / current_action` and `audit_events.new_action` in the action set, `previous_action` NULL or in it, `selected_rule` in the three rules. | Strengthens the stated immutability and action vocabulary; no column, table or name changed. |
| 6 | §4 | `PolicyDecision` has a model validator: outside `DEGRADED_MODE_FALLBACK`, all four actions exactly once, PREPAID_ONLY and MANUAL_REVIEW feasible, selected feasible and within ₹1 of the feasible minimum, `cost_optimal_action` within ₹1 of the overall minimum, and `selected == cost_optimal` iff `selected_rule == "MIN_EXPECTED_COST"` (degraded rules: see #14). `CheckoutRequest(ScoreOrderRequest)` added for the public route. `QueueItem.status/source` and `OrderSummary.delivery_speed` narrowed from `str` to the Literals used elsewhere in §4. | Enforces §9.1 invariants at the contract boundary. |
| 7 | §2 (`/health`) | `/health` (not in §4) returns `policy_version`, the first 8 chars of `policy_config_sha256`, and `demo_clock`; the sidebar renders the policy version from it. | Avoids a hard-coded "v1.0" in the UI. No money, scores or thresholds exposed. |
| 8 | §8.4 | Validator also rejects `floor > cap`, negative `*_inr`, `tie_tolerance_inr < 0`, `block_min_corroborating_signals < 2`, `high_exposure_min_p_abuse` outside `(0, block_min_p_abuse)`, and missing or unknown keys. The slope-order check returns the PREPAID/REVIEW swap region as **info**; any other adjacent slope-order violation is an error. | Review items B13 and the Phase 1 brief. |
| 9 | §1 (frontend) | `vite.config.ts` declares `process` locally instead of adding `@types/node`. | Keeps the dependency list unchanged while injecting `X-Internal-Key` via the Vite proxy from `SENTINEL_INTERNAL_API_KEY`. |
| 10 | §9.1 | `argmin_with_tiebreak` takes the cent-rounded totals (`make_money(total).inr`) rather than raw `CostBreakdown.total`. | Rounding happens before the ₹1 tie comparison so the choice always agrees with the `Money` values stored in the decision and checked by the `PolicyDecision` validator. |
| 11 | §9.1, §9.3 | Policy inputs are a `DecisionContext`: `p_abuse`, `p_return` (record only), V, CLV, the five `EvidenceSignal`s, `decided_at`, `graph_state_as_of`, degraded flag. §9.2 signals are derived by `corroborating_signals(SignalInputs)`, where `SignalInputs` holds the §6.3 feature values plus a link weight per signal and the link kinds carrying any burst. The policy package never reads `sentinel.settings` or the wall clock (AST test). | The contract names the inputs but not their container. The caller supplies `decided_at`: `DEMO_CLOCK` when serving, `placed_at` in the backtest. |
| 12 | §9.2 | `present` = the evidence was observed; `counts_for_corroboration` = it also meets the §9.2 qualifiers. ADDRESS on a multi-tenant address and TEMPORAL_BURST reached only through address links are present but not counted, keep their weight, and say why in `detail`. ACCOUNT_CLAIMS weight is 1.0 (account evidence, not a decayed link; recency is the 180-day window). | Phase 1 review B2/B3: discounted evidence must stay visible to reviewers. |
| 13 | §9.3 G4 | G4 compares `graph_state_as_of` with **`decided_at − 24 h`** (the contract says `DEMO_CLOCK − 24 h`), and its weakness check runs over counted signals only; with no counted signal G2 already removes BLOCK. | Phase 1 review A1: with the fixed demo clock, every backtest decision (D306–D365) lost BLOCK. §11 Demo 3 lists BLOCK removed by G2 and G3 only. |
| 14 | §3, §4, §9.1 G6, §10 | Degraded mode means no model score exists, and none is invented. `DecisionContext.p_abuse/p_return` are None when degraded (and required otherwise); `graph_state_as_of` may be None when degraded. `Scores.p_return/p_abuse`, `QueueItem.p_return/p_abuse` and `PolicyDecision.cost_optimal_action` are nullable; a degraded `PolicyDecision` must have `costs == []` and `cost_optimal_action is None`. `decisions.p_return`, `p_abuse`, `cost_optimal_action` are nullable with `CHECK ((degraded_mode = 1) OR (all three NOT NULL))`. For Phase 6: `AuditEventPayload.p_return`, `p_abuse`, `cost_optimal_action` must be optional. UI (Phase 8): show "No model score, degraded mode" and hide the cost chart. | Phase 1 review B4: costs "calculated" from a placeholder probability would be a deceptive record. `QueueItem` is included so degraded decisions can be listed. |
| 15 | §9.5 | `allow_all()` returns an `Action`, not a `BaselineOutcome` (§4 `BaselineOutcome.strategy` allows only FIXED_THRESHOLD and RULE_BASED). RULE_BASED thresholds are module constants, not config keys. FIXED_THRESHOLD takes τ as arguments; tuning on CALIBRATION is Phase 4. | Keeps §4 and the config key set unchanged. |
| 16 | §1 (frontend) | `react-router-dom` (7.18.4) is a dependency. All frontend dependencies are pinned to exact versions, `frontend/.npmrc` sets `save-exact=true`, and installs use `npm ci`. **Phase 7:** `openapi-typescript` **7.13.0** added as an exact-pinned devDependency (architect-approved, Phase 7 brief §F) with the script `gen:types`; `src/api/types.ts` is generated from `src/api/openapi.json` and no API type is hand-written. **Phase 8:** `vitest` **5.0.1**, `@testing-library/react` **16.3.3**, `@testing-library/jest-dom` **7.0.1** and `jsdom` **30.1.0** added as exact-pinned devDependencies (architect-approved, Phase 8 brief §E) with the scripts `test` and `test:watch`; no runtime dependency changed. | Three routes need a router; §1 omitted it. Exact pins stop `npm install` pulling newer React Flow, Recharts or Tailwind before the demo. |
| 17 | §5 (Generator outputs) | The generator writes one Parquet file per table in `backend/data/`: `events.parquet` holds `order_events`; `accounts.parquet`, `identifiers.parquet`, `orders.parquet` and `order_lines.parquet` sit beside it, with `sim_ground_truth.parquet` and `labels.parquet` as specified. Timestamps are stored as UTC `datetime64`. | A Parquet file holds one table, so the five tables §5 lists under `events.parquet` cannot share one file. Approved by the project owner in Phase 2. |
| 18 | §7.1, §3 `order_labels` | An order that was never delivered (RTO, CANCELLED, or not yet delivered at `as_of`) gets `abuse_status = NOT_MATURED` with `abuse_label` NULL, alongside the NULL return label. | `abuse_status` is NOT NULL and no other value fits: the abuse label matures at delivered + 60 d, which never happens. Keeps `NO_CLAIM` meaning `abuse_label = 0`. Approved by the project owner in Phase 2. |
| 19 | §7.1 (Unresolved) | `UNRESOLVED` (label NULL, excluded) also covers a `QC_FLAGGED` return with no resolution, and any dispute whose `ABUSE_CONFIRMED`/`ABUSE_CLEARED` arrives after delivered + 60 d. Resolutions are only counted inside the adjudication window. | §7.1 defines unresolved only for claims. A QC flag is an open dispute, and without this rule uninvestigated flagged abuse would silently become label 0. Approved by the project owner in Phase 2. |
| 20 | §5 (Archetypes, Outcome generation) | Generator parameters the contract does not set, all named constants in `data/archetypes.py` and `data/generator.py`: **investigation coverage** (Phase 2 review, Fix 2, replacing §5's ring 0.70 / opportunistic 0.50) is 0.95 for a dispute backed by QC_FLAGGED or by carrier evidence that contradicts the claim and 0.60 for any other dispute, for every archetype; 5 % of investigated disputes never resolve; carrier evidence contradicts an abusive claim with p 0.95 (raised from 0.75 in 0.05 steps; 0.95 is the lowest rate giving ≥ 40 CALIBRATION positives with C1–C4 passing) and a genuine one with p 0.02; claims are filed delivered + U(0.1, 5) d; 1 % of orders are CANCELLED; uninvestigated claims are REFUNDED; **rings R1–R3** (Fix 1): 24 / 36 / 30 members, each placing 3–6 orders within 1–6 days of its first, all inside the ring window; no orders outside the window. **R1 and R2 recruit in waves** (Phase 4 decision B1): R1 waves start D40, D60, D80 (8 / 8 / 8 members), R2 waves D170, D195, D220, D245 (9 / 9 / 9 / 9); a member's first order falls in [wave start, wave start + 3] and its orders span ≤ 10 days; each wave has its own devices and tokens (the ring's 6/4 and 9/6 spread evenly over its waves) and every later wave reuses the first wave's first device and first token through 3 members who start on the same day, redrawn until ≥ 2 of them use each (at most 200 draws); R3 has no waves; 6/4, 9/6, 8/5 shared devices/tokens (member m uses device m mod n); shared device on 85 % of orders, shared card on 75 % of abusive card orders; payment mix card 0.40 / UPI 0.40 / COD 0.20; 70 % of abusive orders on one of two ring target products drawn from the ₹8,000–₹40,000 pool; half the members are dormant recruits (account created 90–1,500 d before its first order, no history); R3 opens with a launch burst (half its members start in D315–D318); members rotate through 4 drop addresses of their own; **high-value genuine carts** (Fix 3): 12 % of NORMAL and 15 % of FREQUENT_RETURNER orders are one ELECTRONICS/FOOTWEAR/ACCESSORIES item priced ₹8,000–₹40,000 (discount dropped if it would take the value below ₹8,000), with the archetype's usual outcome process; 25 % of households also share a family device; hostel juniors inherit a senior's phone after ≥ 70 idle days; refurbished-device prior owners are generated as extra NORMAL accounts (2,090 + 60 ≈ 2,150); UNCONFIRMED_ABUSER abuse is undetected wardrobing (QC flag at the genuine 0.02 rate, never investigated), so it is labelled 0 as §5 requires. `sim_ground_truth.archetype` values: NORMAL, FREQUENT_RETURNER, HOUSEHOLD, OFFICE_HOSTEL_PG, REFURB_DEVICE, OPPORTUNISTIC, UNCONFIRMED_ABUSER, RING (with `ring_id`), DEMO (the three demo accounts). Synthetic account and order ids are `ACC-`/`ORD-` + an HMAC prefix of the generator key, so ids carry no archetype order. | The §5 table fixes counts and behaviours but not these rates; the Phase 2 review set coverage, ring scale and high-value carts. Result with seed 20260901 (after the Phase 4 waves and demo changes): 2,940 accounts, 10,150 orders, return rate 23.0 %, confirmed abuse 3.5 % of orders, abuse positives TRAIN 181 / CALIBRATION 37 / TEST 97, UNRESOLVED 79 of 495 disputed orders (16.0 %), 1.7 % of ring orders with an own flagged claim in the prior 180 d (before the waves: 10,164 orders, 22.9 %, 3.7 %, 174 / 45 / 97, 75 of 504, 2.3 %); non-graph TEST abuse PR-AUC 0.555 (see #23). |
| 21 | §5 (R4), §11 | Ring R4's timing is hand-scheduled (`R4_CONFIRMED`, `R4_SCHEDULE` in `archetypes.py`): members 1–3 have QC-flagged returns confirmed on D356, D359 and D362, and every other R4 order is placed on or after D359, so no other R4 confirmation can land before `DEMO_CLOCK`. Carts, prior orders and post-clock outcomes are still sampled. Demo 3's address peer last shipped to the address 150 days before the demo order (link weight 0.04, as §11 states) and was confirmed 125 days before it. The three demo orders are built by `demo_requests()` and are not part of the generated history. | §11 needs exact Demo 2 context (5 device peers, 3 confirmed D356–D362, 3 token peers in 14 d, 4 linked orders in 24 h) that sampling cannot guarantee. For Demo 3, §11's weight 0.04 needs `last_seen` 150 days back, and a confirmation must follow its order. Phase 3 demo fixes (hand-written histories only; no feature definition changed): sampled R4 carts draw from the earbuds and the ₹8,000–₹40,000 pool and never the Demo 2 phone, which appears only on scheduled rows, so Demo 2 sees exactly 3 same-SKU linked orders in 7 d (it saw 8); Demo 1's weekly shoe line is a ₹1,499 pair (`PRD-FOOTWEAR-D03`), putting its CLV at ₹30,616 (was ₹37,276); Demo 3's D305 order is the ₹5,999 earphones instead of a kettle, putting its CLV at ₹15,710 (was ₹11,930). Phase 4 band fix (decision C): Demo 3's device peer is a recent account (created D356) whose first use of the device is D361, within 7 days of the demo order, with orders on D361 and D364 (was created D200 with orders D210–D354); still unconfirmed and concurrent. Demo 3 p_abuse moved 0.046 → 0.114 (band [0.20, 0.70] still missed). Phase 2 open questions, decided (Phase 2 review, Fix 7): Demo 1's order value is ₹4,500.01 (3 × ₹1,666.67 less 10 %), accepted as §11's ₹4,500; Demo 1's token is used only by this account since D1 of the simulation, not for 3 years, accepted. |
| 22 | §5 (Timeline, Rings), §3 `orders.split` | Orders of every hand-authored demo account (the three DEMO-archetype accounts, 59 orders, and since Phase 4 decision F1 their supporting peers `-HH`, `-DEV`, `-ADR`: 13 orders) stay in the world, so the graph and account history see them, but carry `split = "RECENT"` and are never in TRAIN, CALIBRATION or TEST. R3 "exists only in TEST" is enforced on activity: every R3 order is in TEST, no R3 identifier appears on a TRAIN or CALIBRATION order, and no R3 member is confirmed before the first 30 % of R3 orders are placed; a dormant R3 recruit's account may be created before D306 but has no earlier order. Identifier hashing lives in `features/identifiers.py` (normalisation: NFKC, trimmed, whitespace collapsed, case kept; placeholder denylist: empty, unknown/null/none/n/a and similar, all-zero or all-`f` values, and their hashed forms) and the generator imports it. | Phase 2 review Fixes 1, 5 and 6: demo histories must not leak into model splits, and serving code must hash identifiers without importing the generator. |
| 23 | §5 (Outcome generation), §7.1 (Unresolved), §13.3 | Phase 2 review-bound corrections. **C4 bounds set from Fix 2's expected unresolved rate (~19 %)**: UNRESOLVED / disputed orders ≤ 0.20 overall and ≤ 0.25 in TRAIN, CALIBRATION and TEST; RING + OPPORTUNISTIC share of UNRESOLVED ≤ 0.70 (the brief's ≤ 0.10 / ≤ 0.15 could not hold: 0.95 coverage × 0.95 resolution leaves ≥ 9.75 % of evidence-backed disputes unresolved, and 0.60-coverage disputes ~43 %). The §5 rule that 5 % of investigated disputes never resolve is unchanged. **C5 bound relaxed to non-graph TEST abuse PR-AUC ≤ 0.60; measured 0.578** at carrier rate 0.95 when Phase 2 was committed, and **0.555** after the Phase 3 demo-history fixes (#21). No carrier rate in 0.75–0.95 gave both ≥ 40 CALIBRATION positives and PR-AUC ≤ 0.50 (0.95 is the only rate with ≥ 40 positives), and the owner's 0.55 fallback is also exceeded; no further generator change was made. C1 (ring orders with ≤ 1 own flagged claim; 2.3 % have any) remains the primary guard that rings are hidden at account level. **Carry forward to Phase 4**: the evaluation must report the unresolved sensitivity check, with all metrics recomputed treating UNRESOLVED as 0 and again as 1. | Owner decision at the Phase 2 review gate: the original C4 bound was a brief error, not an implementation error. |
| 24 | §6.1–§6.3, §7.2 | Feature-builder choices the contract leaves open (all in `features/`): `feature_set_version` is `fs-1.0-` + the first 8 hex chars of SHA-256 over the sorted RETURN and ABUSE lists (currently `fs-1.0-0ad43d7f`). `account_age_days` is fractional days. The query order's own identifiers count as used by its account at t0 (age 0) for components and sequential-use checks, but are added to the graph only after features are computed. A link's weight is the other account's edge weight on the shared identifier; sequential device use is decided per account pair (the earlier user's last use ≥ 60 d before the later user's first use). Component size, the 200 BFS cap and `min(size, 200)` count accounts (query account included); hops are account hops. **Self-confirmation is not graph evidence** (Phase 3 review): `confirmed_abuse_proximity` measures hops to *other* confirmed accounts only, and `component_abuse_ratio_smoothed`, `device_confirmed_abuse_weight` and the address confirmed-abuse weight leave the query account out of the confirmed count (the ratio's denominator, the component size, is unchanged); an account confirmed before t0 with no confirmed neighbour has proximity 0 and ratio (0 + 1) / (n + 10). The §11 demo feature values are unchanged by this rule. `linked_orders_24h` and `linked_same_sku_7d` count orders by other accounts in the reliable component (Demo 2's 4 linked orders include a member reached in two hops). `device_confirmed_abuse_weight` and the address confirmed-abuse weight have no age window beyond decay; `token_other_accounts_30d` and `address_other_accounts_weighted_30d` use the other account's `last_seen`; `identifier_reuse_velocity_7d` counts every account's first use of the order's identifiers in 7 d. §9.2 link weights are the strongest link per signal; `burst_link_kinds` are the identifier kinds on the BFS paths to the accounts behind the burst, and `burst_weight` the strongest of those paths' weakest edges. Discounted links: at most one per identifier, by precedence HIGH_FANOUT_IDENTIFIER (weight 0), MULTI_TENANT_ADDRESS, SEQUENTIAL_DEVICE_USE, HOUSEHOLD_PATTERN, STALE_RELATIONSHIP. CLV "not-returned" excludes any order with a return or exchange visible at t0; tenure is account age. `features_for_request` refuses a builder whose state already holds anything at or after t0. `definitions.py` also names the graph feature group (`ABUSE_GRAPH_FEATURES`) for the §6.5 counterfactual. `build-features` reads the generator's Parquet files from `backend/data/`. | The contract names the features and rules but not these details; each choice is the narrowest reading that meets the §11 demo values. |
| 25 | §1, §4, §6.4, §9.5, §13.3 | Phase 4 choices the contract leaves open. **Files**: `build-features` also writes `backend/data/policy_inputs.parquet` (SignalInputs fields, `clv_inr`, `graph_state_as_of` = t0, raw `matured_return_rate`, `matured_returns`, `payment_method`, `order_value_inr`; policy and baseline inputs, never model features); `evaluation/report.py` assembles `evaluation.json` and `evaluation/demos.py` scores the demo requests offline until the Phase 6 `ScoringService` exists (`cli score-demos`). **Bundles** also hold `name`, `label` and `hgb_params`; `trained_at` is `DEMO_CLOCK`. **Metrics**: PR-AUC is average precision (step-wise, one point per distinct score; a numpy reimplementation tested equal to `sklearn.metrics.average_precision_score`); the CI is the 2.5/97.5 percentile of 1,000 row resamples (seed 20260901), skipping resamples without a positive; ECE bins split the sorted predictions into 10 equal-count bins; cohorts without a positive omit their PR-AUC keys; ₹10,000 exactly falls in ₹2–10k. **Backtest**: the abusive loss is scaled by `returned_value_fraction` only when `return_label = 1` (labels store 0.0 for item-not-received claims) and the review cost is not scaled; no abusive TEST order has a partial return, so this changes no reported number. `cost_per_detected_abuse` and `precision_block` are reported as 0 when undefined, with a note in `backtest_details`. FIXED_THRESHOLD ties go to the higher τ_block, then the higher τ_review; both τ come from the 0.05–0.95 grid, so the baseline always blocks at p ≥ 0.95. Cost sensitivity re-decides SENTINEL and re-tunes FIXED_THRESHOLD on CALIBRATION under each scaled config. Label sensitivity relabels UNRESOLVED rows at evaluation time only (models are not retrained; return metrics do not use the abuse label). Cold-start recall uses calibrated p_abuse ≥ 0.5; separation uses every FREQUENT_RETURNER TEST order. "No DEMO-account orders" covers every hand-authored demo account (#22). **Phase 4 decisions**: (A) review-bound correction: TEST return PR-AUC must exceed 1.5 × prevalence (was 2 ×); the return model does not drive actions (G1), a score using the true archetype reaches only 0.391 and the frozen return features reached 0.349 (0.386 after B). (B2) §6.4 parameters gain `monotonic_cst`, aligned with the bundle's feature order and stored in each bundle and in `model_registry.json`: abuse +1 on prior_suspicious_claims_180d, device_other_accounts_30d, device_confirmed_abuse_weight, token_other_accounts_30d, address_other_accounts_weighted_30d, component_abuse_ratio_smoothed, confirmed_abuse_proximity, linked_orders_24h, linked_same_sku_7d, identifier_reuse_velocity_7d, component_recent_claims_30d, every other abuse feature 0; return +1 on matured_return_rate_smoothed, prior_returns_90d, n_variants_same_product, every other return feature 0; no −1 (more evidence must never lower risk; removes the non-monotone identifier_reuse_velocity_7d artefact 0 → 0.69, 1 → 0.84, 2 → 0.66; keeps Phase 5 ablation reason codes coherent; applied regardless of demo outcomes). The evaluation-only no-graph model uses the same map restricted to its features. (F3) FIXED_THRESHOLD grid τ_review, τ_block ∈ {0.05, 0.10, …, 0.95, 1.0} with τ_review ≤ τ_block; 1.0 means never (`fixed_threshold` treats a threshold of 1.0 as never firing). (F2) the registry records `os.cpu_count()` at training; the committed-hash reproduction test runs only on the same count. (D) `guardrail_cost`: SENTINEL_UNCONSTRAINED is the expected-cost argmin over all four actions (checked equal to `decide()`'s cost_optimal_action); per guardrail, an order counts under every guardrail that removed its cost-optimal action, with the multi-guardrail count reported. (E) `calibration_bands`: Wilson 95 % intervals; the equal-width ECE uses 10 equal bins spanning [0.05, 1.0] (last bin inclusive) over rows with p ≥ 0.05. **Phase 4 blocker decisions (architect).** (Blocker 1, decision) `test_split_positives`' CALIBRATION bound is lowered from 40 to **≥ 35**; measured 37. The R2 wave 3 schedule is deliberately NOT moved: 37 vs 40 positives makes no material difference to a one-parameter sigmoid calibrator, and regenerating to gain three positives would put the passing Demo 2 at risk. TEST stays at ≥ 70 (measured 97). (Blocker 2, decisions 2.1-2.3) The count bound "TRAIN positives with confirmed-neighbour evidence ≥ 40" was a proxy for the property that the trained abuse model responds to that evidence. Measured directly: calibrated p_abuse is **exactly flat** (delta 0.0000) across the full range of both `device_confirmed_abuse_weight` and `confirmed_abuse_proximity`, on the Demo 2 feature row and on 200 seeded TEST rows, in both the wave-fixed world (TRAIN evidence 52) and the shipped world (21). **The behaviour test is therefore not added** (2.1), and the count test is kept only as a data-health check at **≥ 20** (2.2); measured **21** in the shipped world. **Phase 4's main finding** (2.3): the two features are individually predictive - fitted alone on TRAIN they drive p_abuse **0.026 → 0.800** with 393 and 272 splits - yet receive **zero splits in the full model**, and still zero with the correlated component features dropped or with **`min_samples_leaf` = 5**. `account_age_days` (843 splits), `component_size_reliable_90d` and `primary_category` identify the same rows first (correlation with `component_abuse_ratio_smoothed` **0.76** and **0.86**). The evidence is **redundant, not absent**; no generator change reaches the property while the cheaper proxies remain, and §6.3 is deliberately left unchanged. Phase 5 handles the consequence: see #27. (Decision 3, wave fix) `WAVE_REUSE_DEVICE_MEMBERS` was introduced to widen the reused device from the 3 token-reuse members to 6 per later wave, then **reverted to `WAVE_REUSE_MEMBERS` (3)**; the constant remains so the widening is one edit away. **The shipped world is the reverted one.** Side by side - reverted (shipped): R3 cold-start recall **0.727**, graph uplift **+0.287**, abuse TEST PR-AUC 0.814, TRAIN evidence count **21**; wave-fixed: R3 recall **0.494**, uplift **+0.269**, PR-AUC 0.796, TRAIN evidence count **52**. R3 cold-start recall is a headline demo number and the evidence count only ever proxied the test dropped in 2.1, so the recall wins. **(Fairness, Phase 4 follow-up 3 - recorded only, no behaviour change.)** SENTINEL on TEST burdens new accounts far more than anyone else: accounts under 30 days carry **0.3611** customer friction and a **0.0074** genuine block rate, against **0.0479-0.0586** friction for the 30-365 d and > 365 d cohorts (genuine block 0.0000 and 0.0009). Payment method is effectively neutral: COD **0.0728** friction vs prepaid **0.0851**, which is the outcome §Appendix E asks for by excluding payment method from the abuse model. `account_age_days` **stays in `ABUSE_FEATURES`**: it is genuinely predictive (843 splits, the most-used feature in the abuse model), and removing it would push the same work onto `prior_orders`, which is the same information with a less honest name. The disparity is real and is disclosed rather than engineered away; §Appendix E's stated mitigations (CLV floor, and BLOCK needing graph or claim corroboration rather than newness alone) are what keep the genuine block rate at 0.0074 despite the friction. | The Phase 4 brief fixes the metrics, strategies and fields but not these details. |
| 26 | §11 (Demo 3) | **Demo 3's device fact amended by the architect.** "Device shared with 1 other account (not confirmed), concurrent" becomes **"Device shared concurrently with 3 other accounts in the last 30 days, none of them confirmed abusive."** Applied in `demo_orders.py` only: two more hand-authored peers, `ACC-DEMO-003-DEV2` (created D320, orders D340 and D352) and `ACC-DEMO-003-DEV3` (created D345, orders D355, D358 and D363), each created within 60 days of the demo order, each with 1-3 kept orders, first device use within 30 days of it, none confirmed, neither sharing Demo 3's address or token; all three peers stay split RECENT (#22). Three peers alone did not reach the band (p_abuse 0.0761): `device_other_accounts_30d` **saturates at 1** in the trained model, so the 2nd and 3rd shared account are worth nothing to the score. The lever that works (architect decision 1) is **one existing peer order moved into the 24 h window before the demo order** - the first peer's second order, D364 → D365 20:45, 13.7 h before the demo order at D366 10:25 - giving `linked_orders_24h` = 1. Exactly one order is moved, so TEMPORAL_BURST (which needs ≥ 3) stays absent. No new peers and no retrain: the peers are outside the model splits and TRAIN/CALIBRATION/TEST rows are unchanged (world 2,940 → 2,942 accounts, 10,147 → 10,152 orders, TEST abuse positives 97 throughout). **All four required outcomes hold.** (1) p_abuse **0.6913**, band [0.20, 0.70] - trail 0.046 (Phase 3) → 0.114 (Phase 4 decision C) → 0.0761 (3 peers) → **0.6913** (lever). (2) selected action **MANUAL_REVIEW** (cost-optimal, MIN_EXPECTED_COST). (3) counted §9.2 signals exactly **DEVICE and ACCOUNT_CLAIMS**; **BLOCK is removed by G3 only**, G2 now passing on two counted signals with an anchor. Full guardrail list: **G3, G5** - G5 removes ALLOW on high exposure, which the amendment did not anticipate but which does not affect condition 3 (it concerns BLOCK) and strengthens conditions 2 and 4. (4) margin (ALLOW **₹7,155.07** − MANUAL_REVIEW **₹1,858.14**) / ₹1,858.14 = **+2.851**, required ≥ +0.15. *(Corrects ₹7,155.41 / ₹1,857.52 as first recorded; the engine's `PolicyDecision.costs` and a direct `action_costs` call agree to the cent on the corrected figures. The conclusion is unchanged.)* **Band widened to [0.20, 0.84]** (architect, Phase 4 follow-ups 2 and 1; first set to 0.85, then tightened to 0.84). At 0.6913 the original 0.70 ceiling left 0.0087 of headroom, so any retrain could redden the suite without anything being wrong. The band was only ever a proxy for "MANUAL_REVIEW is the right action"; the action, the counted signals and the cost margin are unchanged and are what the demo actually rests on. **MANUAL_REVIEW is cost-optimal, not guardrail-driven**: with every guardrail disabled, at Demo 3's own V = ₹12,000 and CLV = ₹15,710.45, MANUAL_REVIEW is the cheapest of the four actions at p = 0.20, 0.40, 0.60, 0.6913, 0.75 and 0.83, and BLOCK takes over at 0.90. At the live 0.6913, **BLOCK ₹4,051.94 against MANUAL_REVIEW ₹1,858.14** - BLOCK costs 2.18× review, so cost selects review on its own and G3 is not doing the work. **Measured crossover: MANUAL_REVIEW is cost-optimal for 0.0930 < p < 0.8413** (bisection to 1e-6). §11 states 0.092 < p < 0.837 at its target CLV of ₹15,000; the small shift is the live CLV ₹15,710.45. The ceiling is set at **0.84, just under the 0.8413 crossover**, so the band can never pass while the cost-optimality assertion fails; at 0.85 there was a sliver (0.8413-0.85) where it could. The band must never be raised above the crossover without moving the cost assertion with it. | Architect's §11 amendment at the Phase 4 review: at one shared account no lever inside the old facts reaches the band, and the ALLOW-vs-REVIEW margin (₹994 vs ₹1,175) was too thin to be safe on stage. |
| 27 | §6.5, §11, UI | **Reason codes are evidence statements, not attribution statements** (architect, Phase 4 decision 2.4; **for Phase 5 to implement, recorded here only**). A code fires when its catalog predicate is true, regardless of its individual ablation delta. Every code displays its per-feature delta honestly; where that delta is ~0 the code carries the note *"redundant with other relationship evidence; the score is already explained by correlated features"*. Demo 2's confirmed-device and token-reuse codes therefore fire on evidence, and the **group** ablation (all graph features to reference) is what quantifies the relationship evidence as a whole. The order-detail UI separates two panels: **Evidence** (facts; drives guardrails G2/G3) and **Model attribution** (what moved the score). **Implemented in Phase 5.** `ReasonCode` gains an **optional** `attribution_note: str | None = None` (approved by the Phase 5 brief §C): set to "Redundant with other relationship evidence; the score is already explained by correlated features." when a fired code's own ablation delta is under 2.0 pp. Optional, so payloads written before Phase 5 stay valid. Firing is predicate-driven: `min_attribution_pp` remains in the catalog as the note's threshold, and no longer gates whether a code fires. Measured on the demo rows: Demo 2's `GRAPH_DEVICE_CONFIRMED_LINK` fires at **attribution_pp = 0.0** and carries the note, which is the case this deviation exists for; `GRAPH_TOKEN_REUSE` fires at 0.18 pp and also carries it; Demo 3's `ACCOUNT_PRIOR_SUSPICIOUS_CLAIM` fires at 0.0 pp and carries it. **Phase 9 brief 1.2: the note is now two notes, because the sentence has to be true of the code it sits under.** A code whose predicate reads only `ABUSE_GRAPH_FEATURES` keeps the original wording ("Redundant with other **relationship** evidence; …"); every other code gets "Redundant with other evidence; the score is already explained by correlated features." `reason_codes.predicate_features()` reads the names out of the catalog predicate and `redundancy_note()` picks the wording, so a new code is classified by its own predicate rather than by a hand-kept list. The firing rule and the 2.0 pp threshold are unchanged. Measured over the catalog: RELATIONSHIP for the six graph codes, OTHER for `ACCOUNT_PRIOR_SUSPICIOUS_CLAIM`, `NEW_ACCOUNT_HIGH_VALUE`, `MITIGATING_ESTABLISHED_ACCOUNT`, `MITIGATING_DISCOUNTED_LINKS`, `RETURN_SIZE_BRACKETING` and `RETURN_HIGH_HISTORY`. The two codes the defect was visible on are Demo 1's `MITIGATING_ESTABLISHED_ACCOUNT` and Demo 3's `ACCOUNT_PRIOR_SUSPICIOUS_CLAIM`, and both now read the second wording. **Phase 5 follow-ups (architect, Phase 6 brief Part 1).** (1.1) Because reasons are evidence statements, `reasons` is ordered as evidence: `evidence_strength` STRONG → MODERATE → WEAK, then `|attribution_pp|` descending, then catalog order (§6.5 level 2 said "sorted by |Δ|"). Attribution is not hidden: every code keeps its `attribution_pp`, and `explain_order` also returns `attributions_by_magnitude`, the same codes sorted by `|attribution_pp|` only, for the Phase 8 "Model attribution" panel. Measured on Demo 2: the top reason was `NEW_ACCOUNT_HIGH_VALUE` (WEAK, 94.29 pp); it is now `GRAPH_TOKEN_REUSE` then `GRAPH_DEVICE_CONFIRMED_LINK` (both STRONG), with `NEW_ACCOUNT_HIGH_VALUE` last. (1.2) Architect amendment to the §6.5 `TEMPORAL_BURST` wording: "{n} linked accounts placed orders in the last 24 hours" counted `linked_orders_24h`, which is orders, so it now reads "Linked accounts placed {n} orders in the last 24 hours." (1.3) Templates are count-aware: `{n:singular|plural}` renders the singular only when the named value is exactly 1, and every template with a count uses it, so "1 return or delivery claim(s)" is gone (Demo 3: "The account had 1 flagged return or delivery claim in the last 6 months."). (1.4) `RETURN_HIGH_HISTORY` renders from the raw matured counts: "The customer returned {returns} of {matured} delivered orders. This affects return likelihood, not abuse risk." (was "returns often ({rate}%)" from the smoothed rate, 52 % on Demo 1 against an actual 28 of 48 = 58 %). Firing still uses the smoothed-rate predicate. Without the counts it renders a counts-free sentence (`template_without_counts`). Serving takes the counts from `tabular_features.matured_return_counts`; seeding recovers the matured-order count from the policy-inputs row as `round(matured_returns / matured_return_rate)`, which is exact whenever the code can fire (a smoothed rate ≥ 0.4 needs ≥ 2 returns, so the raw rate is non-zero). (1.5) The latency test asserts median < 20 ms **and** p95 < 40 ms over 60 warmed calls. At the Phase 6 baseline the median had drifted to the budget (18.4-20.2 ms, failing intermittently with the machine ~34 % busy); the bound was not moved. Instead the ~20-row ablation batch is scored on **one OpenMP thread** (`threadpoolctl`, already installed and pinned in `requirements.lock` as a scikit-learn dependency; no package added): fanning 20 rows across 8 cores cost more than the trees. Measured per order: median 18.6 -> **12.4-13.1 ms**, p95 24.7 -> **15.4-18.7 ms**. Predictions are unchanged (the baseline row still equals `train.predict` to 1e-12). The catalog version moves to `rc-1.1`. **Phase 7 brief 1.5:** because `models/attribution.py` imports it directly, `threadpoolctl==3.7.0` is now a **declared** dependency in `pyproject.toml` (architect-approved); `requirements.lock` already pinned exactly 3.7.0, so nothing is installed or re-pinned, and `test_environment.py` asserts pyproject pin = lock pin = installed version. | Direct consequence of the #25 redundancy finding: the confirmed-device link is real and is exactly what G2 counts, so suppressing the code because a correlated feature absorbed the split would hide true evidence from the reviewer. |
| 28 | §6.5 (reason-code catalog) | **`GRAPH_DEVICE_CONFIRMED_LINK` cannot fill its §6.5 template from the point-in-time feature set.** The documented text is "This device was used by {n} accounts later confirmed for return abuse, most recently {days} days ago." Neither value exists outside `features/`, which Phase 5 may not change: §6.3 carries only `device_confirmed_abuse_weight`, a decayed sum, and no confirmed-peer count or confirmation date. The documented template is **kept verbatim** in `reason_codes.toml` and a second field, `template_unconfirmed_counts` = "This device is linked to other accounts since confirmed for return abuse.", renders until the counts exist. `Evidence` already accepts `device_confirmed_accounts` and `device_last_confirmed_days`; supplying them switches rendering to the §6.5 wording with no code change, and a test asserts exactly that. The code fires either way — firing is predicate-driven (#27). | Filling {n} from `device_other_accounts_30d` would tell a reviewer that N accounts were *confirmed abusive* when N is merely the number sharing the device; on Demo 2 that would read "5 accounts later confirmed" when 3 were. A false statement in a reviewer-facing sentence is worse than a less specific true one. **Owed by the phase that can reach graph state** (manual stage 7, scoring service): pass the two values through and the §6.5 wording activates. **Closed in the Phase 6 brief Part 1 (item 1.6).** The FeatureBuilder now outputs two **evidence-only** values for the query order: `device_confirmed_peer_count` (distinct other accounts on this device whose `ABUSE_CONFIRMED` occurred before t0) and `device_most_recent_confirmation_days` (days from the latest such confirmation to t0; None when there is none). They come from the same point-in-time graph analysis (P3/P4: a confirmation at or after t0 is invisible), are not model features (absent from `ABUSE_FEATURES`, `RETURN_FEATURES` and `POLICY_INPUT_COLUMNS`, asserted), and are exposed as `FeatureBuilder.evidence_values()` for serving and as two extra `EVIDENCE_COLUMNS` at the end of `policy_inputs.parquet` for the offline rows. `features.parquet` and every existing policy-inputs column are byte-identical after the rebuild. `AccountState` records every confirmation time so "most recent" means the latest confirmation, not the first. `{days}` renders whole days elapsed (floor), so 3.75 days reads "3 days ago", never a day that has not passed. Demo 2 now renders **"This device was used by 3 accounts later confirmed for return abuse, most recently 3 days ago."** (3.75 days). The counts-free fallback is kept for a missing value. |
| 29 | §6.5 (reason-code catalog, prediction explanation) | Two Phase 5 additions. **(a) New code `GRAPH_DEVICE_SHARED`**, ABUSE / INCREASES / MODERATE, predicate `device_other_accounts_30d >= 3`, text "This device was used by {n} other accounts in the last 30 days." §9.2's DEVICE signal has two limbs — a confirmed-abuse weight **or** 3+ concurrent accounts — but the §6.5 catalog only had a code for the first, so a device that corroborates BLOCK under G2 could produce no device reason at all. That is Demo 3 exactly: 3 concurrent unconfirmed accounts (#26), DEVICE counted, and before this code no device reason was shown. **(b) Exposure features are excluded from the prediction-explanation groups**: `order_value_inr` and `primary_category` stay in the abuse model but are not in the graph / account / order dominance groups. Measured on Demo 2: with them included the "order" group scores **+94.82 pp** against graph's +85.14, purely because ablating ₹24,000 to the ₹1,500 genuine median makes the order look cheap — so the sentence became "driven mainly by the characteristics of this order", which is both useless to a reviewer and wrong about a ring member. Excluding them gives graph +85.14, account −3.98, order −0.20. | (a) Without it the reviewer sees a counted §9.2 signal with no corresponding reason, which is the inconsistency #27 exists to prevent. (b) Order value is *exposure*, not evidence of coordination: it appears in no §9.2 signal, and against a median reference every expensive order would be "order-dominant". Both features remain in the model, where they are predictive; only the explanation's choice of sentence changes. |
| 30 | §7.2 P14, §2 (startup replay) | **Frozen scoring history** (architect amendment to P14, Phase 6 brief §D). P14 says "the serving graph advances only with scored orders: after an order is scored its edges are added". Instead, live-scored orders are **not** added to the in-memory graph: `ScoringService` replays the as-of-`DEMO_CLOCK` history once at startup and every score is computed against that same frozen state. Live orders are still written to the database (`orders`, `order_lines`, new `identifiers`, `decisions`, `audit_events`), and the startup replay reads only `source = 'HISTORY'` orders, so a restart scores exactly as the first start did. Measured: the three demos give identical decisions (every `decisions` column except the two ids) in all 6 scoring orders, each on a fresh database. | Scoring must be deterministic and order-independent: Demo 3 must score identically whether it is scored first or last. With P14 as written, scoring Demo 2 first would add its ring edges to the graph and change what later orders see, so the demo's outcome would depend on click order. Idempotency (P14's test) still holds: a second score of the same order returns the recorded decision and writes nothing. |
| 31 | §3, §4, §9.3 G6/H1, §10, §5 | Phase 6 choices the contract leaves open. **Files**: the §10.1 payload lives in `audit/schemas.py` (the path §10.1 names); service errors in `api/services/errors.py` (`RequestRejected` → 422, `Conflict` → 409, `NotFound` → 404 for Phase 7); seeding in `db/seed.py` (the §1 tree's `seed.py`); `db/models.py` gains `immediate_transaction`, `read_connection`, `utc_iso`, `parse_utc` and a 5 s `busy_timeout`. **Timestamps** are stored as ISO-8601 UTC with microseconds (`2026-09-01T05:00:00.000000+00:00`) everywhere. **Times**: a live decision's `scored_at`, its `decided_at` and its `DECISION_CREATED` `occurred_at` are `DEMO_CLOCK`; `features_as_of` is `placed_at`; seeded decisions use t0 for all of them, as the backtest does. Reviewer actions (override, appeal) carry the **wall clock** (UTC): they are real events, not feature windows; the clock is injectable and tests fix it. **Requests the service refuses** (`RequestRejected` / `Conflict`, never a crash, never degraded mode): `placed_at > DEMO_CLOCK`; `placed_at` at or before the last event of the frozen history (features as of that time would see later events; the demo orders are placed after it); an account not in `accounts` (the table has no `LIVE` source to create one); an order id that already exists without a decision (a history order); the same order id with a different payload (fingerprint = SHA-256 of the request's canonical JSON with `placed_at` in UTC, compared against the request rebuilt from the stored order and lines). **Degraded mode** (#14): triggered by a failed model load or history replay at startup (the service still starts and records why) or by an exception in feature building or prediction; `decide()` itself is outside the guard, so a policy bug surfaces. The record has `features_json = {}`, no reasons, the empty graph summary, prediction text "No model score is available: this decision was made in degraded mode, without a prediction.", model versions from the seeded `model_registry` table when no bundle loaded, and the reason in the G6 detail. Fault injection is a `fault_hook` attribute called at the `features` and `predict` stages; nothing in configuration enables it. **Records**: `reasons_json` and `ScoreOrderResponse.reasons` hold the increasing codes in evidence order followed by the mitigating codes; `min_hops_to_confirmed_abuse` is recovered exactly from `confirmed_abuse_proximity` = 1/(1+hops); `confirmed_abusive_accounts_in_component` by inverting the §6.1 smoothing; `weak_evidence_only` = some signal present and every present signal below the G4 weight 0.3. The idempotent replay is rebuilt from the `decisions` row (full-precision scores) and the `DECISION_CREATED` payload (explanation texts, policy hash), with `status` and `audit_event_id` as they stand now. **Audit**: all floats other than money are rounded to 6 dp before hashing (money 2 dp), and the stored JSON is exactly what was hashed; `verify_chain` also fails a row whose columns (ids, actor, actions, versions, `occurred_at`) disagree with its payload; `events_checked` counts rows up to and including the first broken one. **Reviewer actions**: an override or appeal event is the decision's `DECISION_CREATED` payload with the event fields replaced; G2 counts as unsatisfied when the recorded G2 result triggered or when there is none (a degraded decision), so BLOCK then needs `INDEPENDENT_EVIDENCE_OF_ABUSE` and records `guardrail_conflicts = ["G2"]`; an appeal is opened by a `REVIEWER` actor (support staff), leaves the action unchanged (`previous_action = new_action`), is refused while one is already open, and its reference `APL-<year of the event>-<n:06d>` counts every `APPEAL_OPENED` event under the write lock; `open_appeal` returns an `AppealResult` (order, decision, reference, time, event id) because §4 has no appeal response contract. **Seeding**: the 250 are drawn from all 1,878 TEST orders (no label is read; SENTINEL non-ALLOW **160**: MANUAL_REVIEW 126, BLOCK 19, PREPAID_ONLY 15; ALLOW drawn **90** with `numpy.random.default_rng(20260901)` over the sorted ALLOW ids); probabilities for the selection come from `train.predict` exactly as the backtest's, the recorded ones from `explain_order` (the same model call, equal to 1e-12); `policy_versions.activated_at` is `SIM_START` (v1.0 governs the whole simulation); `model_registry.metrics_json` holds PR-AUC, its CI, Brier and ECE from `evaluation.json`; `calibration_method` is upper-cased to the schema's vocabulary. **Seeded decisions carry no discounted links**: the offline rows do not record them, so `MITIGATING_DISCOUNTED_LINKS` cannot fire on a `BACKTEST_REPLAY` decision and its graph summary lists none (open question for the Architect: one more evidence column would close it). The matured-order count is recovered from the policy-inputs row (#27, 1.4). **Architect decisions, Phase 7 brief Part 1:** (1.1) the open question on seeded discounted links is closed by #32. (1.2) Refusing a `placed_at` at or before the end of the frozen history is **accepted as is**: scoring such an order against the as-of-`DEMO_CLOCK` state would leak every later event into its features, and a per-request replay is ruled out by #30. (1.3) Refusing an unknown account is **accepted for the prototype**: the only live callers are the three demo presets, whose accounts exist; Phase 7 maps the refusal to **404** with a neutral message ("Not found."), never echoing the id. (1.4) The wall clock for reviewer actions is **superseded by #33** (demo reviewer clock). | The Phase 6 brief fixes the steps, contracts and tests but not these details; each is the narrowest reading that keeps decisions deterministic and every record honest about what was and was not computed. |
| 32 | §3 (`decisions`), §4 (`GraphPayload`), §11b | **Point-in-time graph evidence stored with every decision** (architect, Phase 7 brief 1.1). Two nullable columns on `decisions`: `discounted_links_json` (the graph summary's `DiscountedLink` list) and `graph_payload_json` (the reviewer `GraphPayload`), both added to `decisions_core_immutable`. **Seeded decisions:** `seed-db` replays the as-of-`DEMO_CLOCK` world once, chronologically (`FeatureBuilder.iter_points_in_time`), pausing at each of the 250 orders' t0 before that order's edges and that timestamp's events are applied, so the state is exactly what was visible then; it captures the discounted links (which now also reach `assess`, so `MITIGATING_DISCOUNTED_LINKS` can fire on a `BACKTEST_REPLAY` decision) and the `GraphPayload`. Measured: 25 discounted links over the 250 (HOUSEHOLD_PATTERN 12, SEQUENTIAL_DEVICE_USE 7, MULTI_TENANT_ADDRESS 5, STALE_RELATIONSHIP 1), `MITIGATING_DISCOUNTED_LINKS` on 23 decisions, seeding 6.7 → 7.8 s. No seeded action changes (discounted links feed no model feature and no policy input). **Live/demo decisions:** `ScoringService.score()` fills both from the frozen builder at scoring time, inside the G6 guard (a graph-state failure is a degraded decision); a degraded decision stores `[]` and a NULL graph, and the detail route serves an empty `GraphPayload`. **`features/` changes (this item only):** `graph_features.ego_view()` / `EgoView` / `IdentifierView` (the query order's identifiers, every other account's visible use of each, their orders in [t0 − 24 h, t0), and the reliable component) and `FeatureBuilder.iter_points_in_time`, `discounted_links_at`, `ego_view`; no feature value, policy input or `features.parquet` byte changes. **Graph choices the contract leaves open** (`api/services/graph_view.py`): ring 1 radius 200 (identifiers), ring 2 radius 440 (linked accounts, sorted by the kind and id of the first identifier linking them, then id), the current order at radius 100 between the first two identifiers, linked orders 90 outside their account spread by 9°; coordinates rounded to 0.01. Node ids: `ACC:<account id>`, `ORD:<order id>`, identifiers `DEV:`/`ADR:`/`TOK:` + the first 16 hex of SHA-256 over the pseudonymised identifier, so no identifier hash appears in the payload. Labels: identifiers use the table's masked `display_label`; linked accounts and orders `Account ••XXXX` / `Order ••XXXX` (last four characters); the current account and order show their own ids (already on the detail page). Node state: an identifier is `LINKED` if any other account's edge on it is counted, else `NEUTRAL`; a linked account is `CONFIRMED_ABUSE` if confirmed before t0, `LINKED` if any of its edges is counted, else `NEUTRAL`; a linked order is `LINKED` if its account is in the reliable component (so it counts toward `linked_orders_24h`), else `NEUTRAL`. An edge is counted as evidence unless, by precedence, the identifier is high-fanout, multi-tenant, or household-pattern, the device use is sequential, or the link is stale (weight < 0.25 or last use > 90 d before t0) - the reliable-subgraph rule of §6.1; a linked order's `PLACED_BY` edge outside the reliable component carries its account's discount reason, or `NOT_IN_RELIABLE_COMPONENT`. The cap keeps the current account and order, then identifiers, then confirmed accounts, then other accounts by strongest link weight, then their orders; `hidden_node_count` counts every candidate not drawn. The ego graph shows directly linked accounts only, so an order counted in `linked_orders_24h` through a two-hop member (Demo 2, #24) is not drawn. | The 250 seeded decisions had no discounted links (Phase 6 open question 1) and Phase 7 needs a relationship graph for every decision; the frozen as-of-`DEMO_CLOCK` builder cannot produce either for a historical order without leaking later events. |
| 33 | §9.3 H1, §10 (`occurred_at`, `reviewed_at`) | **Demo reviewer clock** (architect, Phase 7 brief 1.4). Reviewer actions (override, appeal) are timestamped `DEMO_CLOCK + (wall time elapsed since the service started)` (`review.DemoReviewerClock`, `time.monotonic`), replacing #31's wall clock. It is monotonic, stays in the demo world, and preserves the real order and spacing of reviewer actions; the appeal reference year is therefore 2026. `ReviewService` constructs one per instance (one per app lifespan); tests inject a fixed clock. Probe logging's 24-hour window uses the same clock. | A timeline that jumps from the decision at 2026-09-01 10:30 IST to the real date looks like a bug on stage (Phase 6 open question 4). |
| 34 | §1 (api tree), §2, §4 (routes, HTTP errors, contracts), §9.4, §13.5, §14.2 | Phase 7 choices the contract leaves open. **Shell:** `create_app(db_path, demo_mode, artifacts_dir, data_dir, clock)`; the lifespan builds one `AppServices` (`api/services/runtime.py`: engine, `ScoringService`, `ReviewService`, `evaluation.json`, the reviewer clock) and raises `DatabaseMissing` "Database not found. Run: python -m sentinel.cli seed-db" when the file is absent. Scoring, reviewer actions, probe rows and demo reset run under one lock (the FeatureBuilder's per-order cache is not thread-safe and FastAPI runs sync routes on a thread pool); reads are unlocked. `api/` never imports `db/seed.py` at module load: the reset route imports it lazily, because seeding reaches the generator. **Errors:** `ServiceError` → 404 `NotFound` / 409 `Conflict` / 422 `RequestRejected`; every 404 body is `{"detail": "Not found."}` and never echoes the id; internal 422 bodies list `loc`, `msg`, `type` only (FastAPI's default echoes `input`); public errors are fixed strings per status ("Not found.", "This request conflicts with an earlier request.", "The request could not be processed."). An unknown account raises `UnknownAccount(NotFound)` (architect 1.3; the Phase 6 test now expects it). **Demo endpoints return 404 when `DEMO_MODE` is off** (Phase 7 brief §B; §4's HTTP-error list said 409). `GET /demo/presets` reads `demo_presets.json`, which `seed-db` writes beside the database from `data/demo_orders.py`, because `data/demo_orders.py` imports the generator's archetypes and serving code may not; `POST /demo/reset` returns `DemoResetResponse {status, decisions}`. `POST /orders/{id}/appeal` returns the `APPEAL_OPENED` `AuditEventOut` ("→ audit event"). `/health` gained a `HealthResponse` model (same fields as #7) so the frontend's type is generated. **Queue:** `action` filters and `counts_by_action` counts the **current** action (what is in effect); `graph_evidence` classifies the relationship signals (DEVICE, PAYMENT_TOKEN, ADDRESS, TEMPORAL_BURST; not ACCOUNT_CLAIMS): NONE = none present, STRONG = one present, counted and weight ≥ 0.3, WEAK_ONLY = present but none strong - a partition of the queue; `exposure_desc` sorts by the recorded expected cost of ALLOW; degraded decisions (no score, no costs) sort last on `p_abuse_desc` / `exposure_desc`; ties by order id; `graph_risk_summary` is a template over the recorded graph summary (confirmed group, counted relationship signals, first discounted link, or none). **Detail:** the customer summary and the RULE_BASED baseline are read from the account's own HISTORY orders and events strictly before t0, replayed into a one-account `GraphState` and read with the same `tabular_features` functions the builder uses (age floored to whole days for display; the baseline gets the fractional age, as in the backtest); CLV basis is NEW_CUSTOMER_FLOOR at or below the floor, CAPPED at or above the cap, otherwise HISTORY; a degraded decision reports the floor its G6 context carried and has no FIXED_THRESHOLD baseline (no score to threshold); `appeal_reference` is the latest appeal's; a decision with no stored graph gets an empty `GraphPayload` at its t0; a live graph's `as_of` is stored in UTC like every other record. **Metrics:** `action_distribution`, `friction_orders` and `manual_review_volume` count the **recommended** action (the system's activity; overrides are in `override_rate` = decisions with at least one `OVERRIDE_APPLIED` / decisions); `explanation_coverage` = decisions whose `DECISION_CREATED` event has both a prediction and a policy explanation; `version_traceability` = audit events whose payload carries the policy version, config hash and all three model/feature versions and agrees with the row's columns. **Checkout:** the outcome follows the decision's **current** action, so a reviewer override reaches the customer (§11 Demo 3: override to PREPAID_ONLY → PREPAID_PAYMENT_REQUIRED); the public route scores with source LIVE (an order already scored as DEMO replays idempotently); `support_reference` = `SUP-` + the first 8 hex of SHA-256("support:" + decision id), upper-case. **Probe logging** writes one row for every call that passes request validation, whatever the outcome (a malformed device id is never logged, since it may be raw PII): `attempts_24h` = 1 + the device's rows in [now − 24 h, now) on the reviewer clock; `distinct_carts_24h` = distinct order ids tried from the device - this call's plus the device's LIVE/DEMO orders placed in that window - because `probe_events` has no cart column and the schema may change only for Part 1. **OpenAPI:** `python -m sentinel.cli export-openapi` writes `frontend/src/api/openapi.json` (indent 2, UTF-8, LF); a test fails when it differs from a fresh export. **Test isolation found at the gate:** with `tests/api` now running before `tests/model`, `test_committed_artifacts_reproduce_on_the_same_cpu_count` failed in the full suite and passed alone. Measured: in one interpreter, loading the committed bundles before training changes the SHA-256 of the freshly pickled bundles, while every prediction on the full offline frame is identical (`np.array_equal`, both models); a clean interpreter reproduces the committed hashes exactly. The test now trains in a fresh subprocess, as `cli train` does, and asserts the same SHA-256 equality; no bound or comparison was changed.  **Phase 8 brief Part 1, recorded here, no code change.** (1.3) The two action counts answer different questions and stay as they are: `/metrics` `action_distribution` (with `friction_orders` and `manual_review_volume`) counts the **system recommendation**, because it measures what the policy engine decided; the queue's `action` filter and `counts_by_action` count the **current action**, because a reviewer working the queue needs what is in effect now. The order-detail page labels each one explicitly rather than hiding the difference (Phase 8 Part 2 B1: the decision card shows `current_action` as the badge and "System recommended: X" under it whenever an override moved it). (1.4) Probe logging still writes a row only for a request that passes validation. Storing a malformed device id would mean storing whatever the caller sent, which may be raw personal data, in a table (`probe_events`) that has no retention story and is not covered by the §4 `HashedId` guarantee that raw PII cannot enter the system. A prober sending garbage is therefore not counted; that is the accepted cost, and it is the smaller one. |
| 35 | §4 (`GraphPayload`), §11b (graph layout) | **The reviewer graph must agree with the counts printed beside it** (architect, Phase 8 brief 1.1; closes Phase 7 open question 1 in favour of option (a)). `GraphPayload` gains two integer fields, **`linked_orders_24h_shown`** and **`confirmed_peers_shown`**: what the drawn graph actually accounts for. The ego graph is no longer limited to directly linked accounts. Every account whose order is counted in `linked_orders_24h` (any reliable-component account other than the query account, at any hop) and every account counted in `device_confirmed_peer_count` is drawn, together with the identifier nodes that connect it. `features/graph_features.py`: `Reached` records the BFS parent (`via_account`, `via_ident`); `EgoView` gains `depth_of`, `parent_of`, `path_edges` (a new `PathEdge` value object: one account's use of one identifier on such a path, with its own discount reason), `must_draw` and `confirmed_device_peers`; `recent_orders` now covers every component account, which is exactly the set `analyse` counts. No feature value, policy input or `features.parquet` byte changes. `api/services/graph_view.py`: an account reached through a further hop sits one ring further out (`HOP_RADIUS` 280, the connecting identifier half way, siblings spread `HOP_SPREAD_DEG` 14°); the 40-node cap keeps the current account and order, then the order's own identifiers, then the accounts and identifiers the counted evidence needs, and only then other linked accounts (confirmed first, then by strongest link weight), so a counted account is never dropped while an unrelated one remains; an account whose connecting hop was dropped is dropped with it rather than drawn floating. **A second case the fix also covers:** an account can be linked through an identifier the current account used on an *earlier* order (a COD order has no token of its own, and §6.3 falls back to the account's prior tokens), so the graph can now show more than one DEVICE or PAYMENT_TOKEN node next to the current account. Those links are real and counted; the order's *own* device is identified by its id, not by adjacency. Stored payloads are regenerated by `seed-db`. Measured: Demo 2 draws **4 of 4** linked 24 h orders (was 3 of 4; the fourth comes from a member two account hops out) and **3 of 3** confirmed device peers, at 17 nodes and 18 edges, not truncated; Demo 1 0/0 at 6 nodes; Demo 3 1/1 at 9 nodes; over the 250 seeded decisions every graph's drawn counts equal `linked_orders_24h` and `device_confirmed_peer_count`, none truncated. | Phase 7 open question 1: the detail page prints "4 linked orders in the last 24 h" beside a picture showing 3, and a judge counts. Option (b) (a caption saying "directly linked") would have made the page honest but left the evidence invisible; the counted member is the point of the demo. The `*_shown` fields keep the page honest in the one case the picture still cannot be complete, which is truncation. |
| 36 | §1 (api tree), §4 (contracts) | **`GET /api/v1/internal/policy`** (Phase 8 brief 1.2): the loaded policy configuration, read-only, internal key required. New router file `api/routers/internal_policy.py` (beyond the §1 tree, like `health.py` under #7) and three contracts in `api/schemas.py`: `PolicyAssumptionValue` (key, value, and a `Money` for every `*_inr` key), `PolicyAssumptionSection`, `PolicyAssumptionsResponse` (`policy_version`, `policy_config_sha256`, `notice`, sections). The sections and keys are enumerated from `policy/config.py`'s section map (renamed `_SECTIONS` → `SECTIONS`) and the typed dataclasses' fields, so the response cannot drift from what the policy engine actually loaded, and a test asserts value-by-value equality. Money is formatted once, server-side, as §4 requires. | The "Demonstration assumptions" panel (§11b, B3) has to show real numbers. Without this route the frontend would have to carry policy v1.0's costs, rates and guardrail thresholds as literals, which the brief forbids and which would silently go stale against the config hash the same page prints. |
| 37 | §1 (frontend tree), §11b (palette), §13.6 | Phase 8 choices the contract leaves open, all in `frontend/`. **Test harness** (brief §E, approved): `vitest` **5.0.1**, `@testing-library/react` **16.3.3**, `@testing-library/jest-dom` **7.0.1** and `jsdom` **30.1.0** as exact-pinned devDependencies (also recorded in #16), with `npm test` (`vitest run`), `vitest.config.ts` and `src/test/setup.ts`. jsdom has no layout engine, so the setup file gives the document a fixed 1440x900 and makes `ResizeObserver` report it once per observed element; nothing it does changes what a component computes. §13.6 said "no Playwright unless time allows" and is unchanged: these are component tests, and the browser pass in §F is a one-off script run from outside the project, adding no dependency. **Palette**: §11b's tokens are declared once in `index.css` under `@theme`, plus two the contract does not name - `--color-neutral-meter` (slate-400, the abuse meter below any friction band) and **`--color-block-text` (red-400)**. red-500 measures **4.46:1** against slate-950 and **4.08:1** against slate-900, under the 4.5:1 WCAG AA threshold for body text, so red-500 paints fills, bars, rings and confirmed-abuse nodes while red-400 (**6.0:1** / **5.5:1**) carries small red text. Both are BLOCK-only; nothing else on the page is red. ~~**The 70 % band on the abuse meter is read from `GET /internal/policy`** (`guardrails.block_min_p_abuse`), not written into the frontend; the 50 % band is a display midpoint with no policy counterpart. Until the configuration loads the meter makes no red claim.~~ **Superseded by the Phase 9 brief 1.1 (the architect's own correction to the Phase 8 brief B2): the abuse meter is one neutral token at every value.** Reading the band from the API kept the *number* out of the frontend but still let position on the meter encode a policy threshold, which DESIGN.md §2 and §7 forbid outright, and it duplicated the action badge's job. `AMBER_FROM_PERCENT`, the block band and the `blockFromPercent` prop are gone; `lib/policy.ts` keeps only `usePolicyAssumptions` (the assumptions panel still needs the payload), so `policyValue` and `blockConfidencePercent` went with the band. The ghost marker for `p_abuse_without_graph_evidence` is unchanged. Measured: the meter's fill is the identical `var(--color-neutral-meter)` on Demo 1 (<0.1 %), Demo 3 (69.1 %) and Demo 2 (95.1 %), which straddle the old bands, and no action colour token appears anywhere in the abuse card. **Routes**: `pages/OverviewPage.tsx`, `pages/QueuePage.tsx` and `pages/OrderDetailPage.tsx` are separate files as §1 shows (they were inline in `App.tsx`); the Overview and Queue placeholders are unchanged in content, and `components/SyntheticDataBanner.tsx` moved out of `App.tsx` too. `Panel.tsx` (surface, field, skeleton), `AssumptionsPanel.tsx`, `ReviewerPicker.tsx`, `lib/actions.ts` and `lib/policy.ts` are beyond the §1 component list. **Graph**: each node declares its box and one source and one target handle at its centre rather than being measured, so every edge is a straight centre-to-centre line that is identical in every environment and does not wait a frame for layout; the server's coordinates are treated as node centres (React Flow positions by the top-left corner). React Flow's own `fitView` prop waits for `nodesInitialized`, which needs measured handle bounds, so the fit is driven from the instance on mount instead. Wrapping the flow in a `ReactFlowProvider` drops every edge under this arrangement and is deliberately not used. **The no-hard-coding scan** (§E) compares the numeric literals in every `.ts`/`.tsx` file under `src/` - minus generated types, fixtures and tests - against every value of the recorded `/internal/policy` payload, ignoring numbers inside string and template literals (a Tailwind class `duration-150` is not ₹150; rendered money is covered by its own test) and excluding **{0, 1, 2, 100}** as ordinary programming constants that collide with a guardrail's signal count and a support cost. It was verified to fail on a planted `0.70`. Every rate, rupee amount and probability threshold in the configuration is checked, as are the strings `v1.0`, `fs-1.0` and `return-/abuse-hgb`. **Fixtures** under `src/__fixtures__/` are recorded verbatim from the real backend (world generated and seeded, the three presets scored through `POST /score-order`, each `GET /orders/{id}` body written out; the degraded one from the real G6 path, the overridden one from a real override). One cast, in the test harness, turns a JSON import into its contract type. **After an override the detail refetches silently**, without the loading skeleton, so the dialog showing the server's `warnings` is not unmounted by its own success. | The Phase 8 brief fixes the layout, the palette's roles and the tests but not these details; each is the narrowest reading that keeps every number on the page traceable to the API and the page honest at the contrast the brief asks for. || 38 | §4 (`Money`), §6.5, §9.3 G3/G5, §9.5, §11b | **Probabilities in reviewer-facing text are formatted once, server-side, exactly as the page formats them** (Phase 9 brief 1.3). New helper `format_probability()` beside `format_inr` in `sentinel/money.py`: a percentage with one decimal, `<0.1%` for a non-zero value below 0.1 %, `0%` only for an exact zero. Used in `policy/guardrails.py` (the G3 and G5 details) and `policy/baselines.py` (`rule_fired`), and the variable name `p_abuse` is replaced by the words "abuse probability" in every string a reviewer reads. G3 now reads "G3 requires an abuse probability of at least 70.0%; this order scored 69.1%." and, when it passes, "An abuse probability of 95.1% meets the 70.0% minimum for BLOCK."; G5 reads "G5 removes ALLOW when the abuse probability is at least 40.0% and the order value is at least ₹10,000."; FIXED_THRESHOLD reads "abuse probability >= 5.0%". Action enum names inside the explanation are left alone (§6.5's own example uses them; the design pass decides). **Two choices this forced.** (a) Unlike `format_inr`, which quantizes the decimal as written with ROUND_HALF_UP, `format_probability` rounds the binary float, because that is what the browser's `(p * 100).toFixed(1)` does; a test walks all 10,001 points of a 1e-4 grid and asserts the two agree at every one. Half-up on the written decimal is the better rounding rule and is what money keeps, but a probability in a server sentence sits directly under the same probability on a card, and the two agreeing is the whole point of the item. (b) `_truncate_3dp` is removed, and the guarantee it carried — the G3 sentence can never show the threshold as reached when it was not — moves into G3 itself: at one decimal a score just below the threshold can round onto it, so when the two render identically G3 says "this order scored just under 70.0%" instead of printing the same figure twice. `test_c5_g3_detail_below_threshold` still pins the guarantee, now at 0.6951 → "69.5%" and 0.6999 → "just under 70.0%". | The explanation read "G3 requires p_abuse of at least 0.70; this order scored 0.691" beside a card reading 69.1 %, and Demo 1 read "scored 0.000" beside a card reading `<0.1%`. Two formats for one number on one screen, and a raw variable name in a sentence meant for a reviewer. |
| 39 | §11b (graph layout), DESIGN.md §4 | **Timestamps render in the demo's timezone** (Phase 9 brief 1.5, closing Phase 8 open question 3). `formatTimestamp` and `formatDate` pass `timeZone: 'Asia/Kolkata'` — the offset `DEMO_CLOCK` carries — and `formatTimestamp` suffixes "IST". The vitest suite runs under `TZ=UTC` (`vitest.config.ts` `env`), so a regression to the browser's zone fails rather than passing on a machine that happens to be in IST. Measured: Demo 1's placed-at reads "1 Sept 2026, 10:25 am IST" under `TZ=UTC`, where it previously read 4:55 am. **1.6, same commit:** text the frontend composes uses the labels in `lib/actions.ts` — the decision card's "System recommended: Manual review", the audit timeline's "Manual review → Prepaid only", and humanised override reason categories ("Customer verified"). Sentences the server wrote (the policy explanation, guardrail details) are rendered verbatim, enum names included, because they are the record. **1.7, same commit:** the evidence-strength tag is `self-start`, so it no longer stretches to the row height. | The screenshots were taken in a UTC container and read 4:55 am for an order placed at 10:25 IST, which made the rehearsal machine's clock settings load-bearing for what a judge sees. |
| 40 | §4 (`GraphNode.flags`), §11b (graph layout) | **`RECENT_24H` is stated once in the legend instead of on every ORDER node** (Phase 9 brief 1.8, closing Phase 8 open question 1). The badge is filtered out of ORDER nodes only; the legend's order swatch reads "Order placed in the last 24 h". Linked ACCOUNT nodes keep the badge, where it means that account placed an order in the window — a different fact the order swatch does not carry. Node markup gained `data-node-kind` so a test can scope to ORDER nodes. The panel height is untouched (design pass). **The brief's pre-check, measured before anything was changed:** over the 250 seeded payloads, ORDER nodes total **457** — **207 linked, all 207 carrying `RECENT_24H` (zero exceptions)**, and **250 current-order nodes carrying no flag at all**. The current-order node is the order under review drawn at the centre, is always that decision's own order (asserted), and never had a badge to remove, so the brief's "every ORDER node carries RECENT_24H" holds for every node that renders a badge and fails only for the node that renders none. Rather than halt the phase on that technicality or quietly relax the check, the test asserts both halves explicitly and the legend's CURRENT swatch was relabelled "This order and its account" so no swatch describes a node it is not about. **Flagged for the architect** in the Phase 9 report, open question 1. | Phase 8 open question 1: the per-node badges forced an extra ring of spacing and repeated, once per satellite, a fact that is true of every satellite. |
| 41 | §1 (frontend tree), §4 (`QueueFilters`), §11b | Phase 9 (MVP) choices the contract leaves open, all in `frontend/`. **Files** beyond the §1 tree: `components/SimulateCheckout.tsx` (the §B preset panel and the §C reset, together because §C says the reset exists only when the panel does) and `lib/queue.ts` (the filter state). **Filter state lives in the URL query** (§A): `lib/queue.ts` mirrors the `QueueFilters` field names exactly, so the page's query string is the API's query string and there is no second vocabulary; default values are omitted from the browser URL, so an unfiltered queue reads `/queue`, while the API query always carries `limit` and `offset` explicitly. Changing any filter resets `offset` to 0, because page 3 of the old filter means nothing under the new one. `min_p_abuse` and `min_value_inr` are `QueueFilters` fields the brief's filter list does not name and no control is rendered for them; they stay available on the API. **Page size** is the contract's own default, 50, sent explicitly. **Counts and the action filter are labelled "current action (after review)"** in words on the page, per #34 1.3. **Probability columns** reuse `formatProbability`, so a rate renders `<0.1%` rather than being rounded to zero; a degraded row renders `No model score` in both columns and invents nothing (#14). **Backtest rates** reuse the same helper and `manual_reviews_per_1000` uses a new `formatOneDecimal`, which is not money and not a probability. **The Overview reads the policy version from `/health`** (#7) rather than from the metrics payload, which does not carry one; a new `getHealth()` sits in `api/client.ts` because `/health` is not under `/api/v1`. **No backtest row is highlighted**: every row carries one identical class string, asserted by test, and no action colour token appears inside the table. **Tests**: `__tests__/queue.test.tsx` (19) and `__tests__/overview.test.tsx` (15) against payloads recorded from the real backend into four new fixtures (`queue.json`, `presets.json`, `score-order-response.json`, `metrics.json`); the harness gained `stubApi` (which records every request, so a test can assert the preset body posted was byte-identical) and `renderApp`, which mounts the whole app at a route so the synthetic-data notice is checked on all three pages. The no-hard-coding scan picks the three new source files up automatically and was confirmed to do so. **Browser pass**: `playwright-core` was installed with `--no-save` for the §F run and `package.json` and `package-lock.json` are unchanged, as in Phase 8. | The Phase 9 brief fixes the columns, filters, panels and states but not these details; each is the narrowest reading that keeps the page's numbers traceable to the API and its two action counts honestly distinguished. |
| 42 | §1 (repository structure) | **`scripts/setup_env.sh`**, a top-level `scripts/` directory the §1 tree does not list: one idempotent script that brings a fresh checkout to the state the suite and `serve` expect — venv, `pip install -r requirements.lock`, the editable `--no-deps` install, `npm ci`, then `generate`, `build-features`, `seed-db`. Every step checks for its own output and is skipped when it is already there; the dependency step, whose output is installed packages rather than a file, is stamped with the SHA-256 of the lockfile it installed (`backend/.venv/.requirements.lock.sha256`, inside the git-ignored venv), so a changed lockfile re-installs and an unchanged one does not. **It changes no dependency, no pin, no tracked file and no runtime code path**: it only runs the commands the README already documents, in the order it documents them, and everything it writes is git-ignored. Requires Python 3.12 and refuses to proceed without it, because `requires-python` is `==3.12.*` and the committed artifacts load only under the pinned scikit-learn. No SessionStart hook and no CI wiring. Verified: a second run skips every step; deleting one output re-runs that step alone. | Operator request after the Phase 9 gate. A fresh container had no venv, no `node_modules` and no `backend/data/`, and bringing it up took a dozen commands recovered by reading the README; the next builder should not have to re-derive them. |
| 43 | §4 (routes), §11, #34 (demo endpoints, checkout source) | **Presets are scored as DEMO** (Phase 10 Part 1). New `POST /internal/demo/presets/{order_id}/score` (internal key; `DEMO_MODE` only, 404 otherwise): it finds the preset by `order_id` in `demo_presets.json`, scores it with source `DEMO` and returns `ScoreOrderResponse`; an unknown preset is 404 "Not found.". The Queue's Simulate checkout buttons call it with no body. `POST /score-order` and the public checkout still record `LIVE`, and a preset already recorded replays under either source. `test_openapi_has_no_model_endpoint` now allows exactly these two scoring paths and asserts both return the whole `ScoreOrderResponse` (a recorded decision, never a bare score). | The schema already had `DEMO` for exactly this; the queue showed the presets as "Live". |


## Carried-forward TODOs

Commitments made when a deviation was accepted, owed by a later phase. A phase that closes without
clearing its line here has not finished. Do not remove a line; mark it done with the commit that did it.

- **TODO (Phase 11 = operating-manual stage 12, Backtest depth):** the account-age cohort friction table from #25 must be displayed in the
  dashboard — friction rate and genuine block rate by account-age band, beside the multi-tenant, payment
  method, order-value and category cohorts §Appendix E asks for. The numbers exist in `evaluation.json`;
  Phase 11 owes the surface, not the measurement. (First written as "Phase 11, Overview"; Overview is
  spec phase 10. The cohort table belongs with the backtest panel, as the operating manual's stage 12 says.)
- **TODO (Phase 13, Hardening):** the model card must state the new-account friction disparity plainly —
  accounts under 30 days carry 0.3611 friction against 0.0479–0.0586 for older cohorts (#25), that
  `account_age_days` is retained deliberately, and why. It must not be softened into a general note about
  cold-start uncertainty.
- **TODO (manual stage 7, scoring service):** pass `device_confirmed_accounts` and
  `device_last_confirmed_days` into `models.explain_order` so `GRAPH_DEVICE_CONFIRMED_LINK` renders its
  §6.5 wording instead of the counts-free fallback (#28). The parameters already exist and are tested.
  **Done** in `phase 5: follow-ups` (Phase 6 brief item 1.6): the builder supplies both values and the
  scoring service and seeding pass them through.

## Withdrawn review requests

Requests from a review that were considered and then withdrawn. They are not deviations; they are recorded so the decision is not revisited.

| Review | Request | Decision |
|--------|---------|----------|
| Phase 1 review, C7 | Demo 1's policy explanation should contain no "not permitted" text. | **Withdrawn.** It conflicts with the §6.5 explanation style, whose Demo 3 example reports "BLOCK was also not permitted" although BLOCK was not a contender, and with C7's own Demo 2 expectation ("ALLOW was also not permitted"). Demo 1 has BLOCK removed by G2 and G3 in the same way, so the explanation keeps the §6.5 style and reports it. `test_c7_demo_1_explanation` asserts the full sentence. |
